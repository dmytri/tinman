> STOP. Captain's notes: non-binding. Captain writes, Captain trims. Anyone else: close this file now.

# Captain Notes

Binding behaviour lives in `.feature` specs and referenced scantlings. History lives in git. These notes carry only what the next cycle needs.

## First action

Run the Opening retrieval first and believe it over anything here.

## Naming the root had a consequence nobody predicted, 2026-07-28

**Captain specced the root region to carry a name, correctly, from the ARIA rule that `application` requires one. That turned the whole screen into an addressable scope and a refusal became a bind.** `scope_of` credits the containing region, so a proposal for a pane resolved its scope to the root and `Locator::nth(...).within(root)` bound a region the deterministic pass had just refused. Crew fixed it by narrowing against the root's children rather than the root itself: scoping to the root selects exactly what an unscoped locator selects, so it reports a narrowing that never happened. **A correct spec change reached a seam three steps away, and no reading predicted it; the focused run did.**

**The `within` defect was deeper than the wire key.** Beyond the schema naming `within` while production declared `scope`, `activate` in `src/flow.rs` was discarding the recorded narrowing entirely, so scoped locators replayed unscoped no matter which key they carried.

**Third variant of the declared-but-unspecified pattern, and the least visible.** `anchored`/`elastic` was prose promising a model property that never existed. The alternate screen was prose promising behaviour production never had. **`capture` is declared in the plan schema and implemented in `src/plan.rs`, and no scenario has ever exercised it** - unspecified behaviour rather than a broken promise, and the round-trip check fails on it because a property nothing carries is indistinguishable from a property nothing supports. It earns a scenario rather than deletion, being the capture limb of the model's own test and already a driver method.

## Captain ran two Quartermasters at once, 2026-07-27. Do not repeat it.

**Both were dispatched against the same base and both wrote `tests/cucumber.rs`.** The first spent real effort trying to establish who authored step definitions that appeared in its tree mid-run, reported it as an unexplained arrival, and could not settle it from the tree because the author was the other QM. **Dispatch one QM at a time.** The watchbill is a single ordered scope and two roles working it concurrently produce exactly this: work that looks like contamination, and a report that cannot be trusted about authorship. The operator's rule that an idle deck is worse than a wrong call licenses dispatching promptly; it does not license two of the same role.

**One good thing came of it, and it is a durable check rather than luck.** The concurrent arrival carried a hardcoded `ROLES_REQUIRING_A_NAME` restating `scantlings/tom.schema.json`, and it had already drifted, demanding a name for `table` and `columnheader` which the scantling does not. The other QM caught it and replaced it with a read of the scantling. **A constant restating a scantling is a second copy of a durable contract, and it drifted within hours of being written.**

## The alternate screen was claimed in prose and absent from the code, 2026-07-27

**Third instance today of a `Rule:` asserting a capability production does not have**, after `replay.feature`'s anchored and elastic edges, and the model narrated as deriving roles it recognises. `rg AlternateScreen|1049 src/` returns nothing: the assistant never switches screens, while the feature's Rule says it holds the terminal in raw mode on the alternate screen and the recorded design says alternate screen for the session with the transcript written into scrollback on exit.

**Two scenarios passed vacuously because of it.** Captain's `And the alternate screen has been left` is true of a program that never entered one. And `the transcript is written into the scrollback` asserts the text is on screen after `esc`, which is trivially true when no screen switch ever happened: **scrollback is only meaningful because an alternate screen was left, and with none there is nothing to write back into.**

**Closed with positive assertions rather than absences**: the assistant draws on the alternate screen, and the operator's own scrollback is not on screen while it draws. Both go red against today's code.

**The pattern worth carrying: prose in a `Rule:` is not free.** Three times today a Rule described a capability nobody had built, and in two of the three a scenario passed vacuously beside it because the assertion was shaped as an absence. Absence assertions are the ones that go quiet.

## QM's four blockers, resolved 2026-07-27. One of them was Captain writing a check that cannot fail.

**1. The binding scenario was a tautology, and Captain wrote it while closing a hole about checks that cannot fail.** `And every locator bound a region of the role and name the plan recorded` compares a thing against itself: `src/tom.rs` `resolve` filters on role and name equality, so `Resolution::One` guarantees the match. Its `Given` also built a plan carrying no locators, so it passed over an empty set. **The falsifiable element is the rung, which Captain missed**: `src/plan.rs` records `scope` and `binding`, and `src/flow.rs` resolves with `Locator::new(role, name)` and discards both, so a plan recorded needing narrowing replays without it. Replaced with a scoped-locator scenario that goes red on that real defect.

**2. Not an environment fault, and QM's diagnosis was wrong.** `.env` carries `TINMAN_API_KEY` and the provider answers HTTP 200, verified. The budget scenario is red because the tagline seam is slow, and it is slow because reasoning-off has not landed. **The budget contract is correctly red about a real, queued defect**, which is what it was written to do. No fitting out is owed.

**3. Protocol drift, and the attestation could not see it.** The driver serves `wait`; `driver-protocol.schema.json` did not name it, so a `wait` message failed the protocol it is served under while the `@contract` scenario stayed green by exchanging only launch and tom. `wait` added to the enum, and a new contract scenario exchanges **every** method the scantling names, so a name nothing exercises can no longer hide.

**4. The first `unsafe`, and it routes to a crate.** `src/assistant.rs` declares `signal` against the linked C library, and `hand_the_terminal_back` runs inside a signal handler where only async-signal-safe calls are legal, which is the code that works in testing and deadlocks in the field. Crew's reasoning was defensible, the library being linked already, but the anti-NIH rule names this case and signal safety is a maintained crate's whole job. **`signal-hook` 0.4.4 selected, recorded under `## Dependencies`, install routes to Shipwright.**

**Harbour findings QM reported, not acted on.** `src/record.rs:210` holds raw mode with the same interrupt exposure the assistant just closed, and no current target names it. The three-string sibling of the printing step builds its command the way the two-string one did before QM fixed it, and only its current data keeps it green.

## Why this project is possible now and was not before. Operator framing, 2026-07-27.

**Inference is the ingredient that makes the whole thing buildable, and the pairing rather than the inference is what makes it usable.** Terminal accessibility is an empty category because rule-based structure recognition over arbitrary terminal programs does not work: the idioms are a catalogue with no end, `ls -la` against `top` against `df` against `git log --oneline`, and this voyage walked into exactly that with column detection. Inference covers what a rule set cannot finish. Inference alone would be a guessing machine and nobody tests against a guess.

**The division that makes it trustworthy: inference for coverage, determinism for trust.** The grid determines what it determines, cursor, attributes, geometry and exit. Inference proposes what convention decides, which idiom this is, where the columns fall, what a region is called. The screen refuses whatever inference cannot back, and replay invokes no model at all. Neither half suffices alone: a rule set cannot reach arbitrary programs, and a model cannot be trusted to write tests. **Together they reach arbitrary programs and still produce artifacts that replay deterministically, which is the combination that did not exist to be built before.**

**It also answers the outside review's sharpest objection.** A maintained idiom catalogue is what rules produce when they must cover everything, and the answer is not a better catalogue: it is to stop maintaining one wherever inference generalises and a deterministic pass refutes.

## Three findings from QM, 2026-07-28, and the first is Captain's own process fault

**The deck moved under verification for the third time, and Captain caused all three.** Specs were edited while QM was running and then committed mid-run, so its greens for watches 1-16 were measured against text that has since changed and are not evidence for those watches as they now stand. They need re-running on a fresh dispatch. **The operator's rule that an idle deck is worse than a wrong call licenses dispatching promptly; it does not license editing the scope a role is currently measuring.** Author between dispatches, not during them.

**A scenario contradicted a scantling, and the attestation stayed green because nothing tried.** `the tagline request asks the provider not to reason` requires the request to disable reasoning; `inference-request.schema.json` closed to additional properties over exactly `url`, `authorization`, `model`, `temperature` and `messages`, so no wire shape could satisfy both. QM correctly left the step undefined rather than guess. Closed by adding an optional `reasoning` object, proven to admit the disabled form and still reject a bogus field. **Recorded honestly: `reasoning` is OpenRouter's parameter and not a cross-vendor standard, so this is the one place the vendor-neutral claim strains.** An OpenAI-compatible endpoint that does not know it ignores it.

**A fourth schema-against-production mismatch, in `capture`.** The plan schema requires `role`, `items` and `as`; `src/driver.rs` names the pane under `within`. The same shape is spelled two ways in two places, which is the family that already produced `within` against `scope`. The round-trip contract scenario is the check that should catch it once `capture` is exercised.

## Captain committed the durable artifacts directly, 2026-07-28. A named departure.

**What and why.** Twenty-one durable artifacts carried a full session of specification work with no copy anywhere: eleven `.feature` files, five modified scantlings, two scantlings existing only as untracked files, the watchbill, `RIGGING.md`, `AGENTS.md` and the help asset. Production is regenerable from them and they are regenerable from nothing, so the exposure was the only real risk on the board.

**Why not Boatswain.** Custody is owed when the watchbill is spent, and under the never-split ruling it will not be spent until three unbuilt capability areas are green. Boatswain would have refused correctly, and the specs would have stayed uncommitted for as long as that takes.

**What was deliberately left out: `src/` and `tests/`.** Role-advanced production and verification stay with Boatswain, which owns the recheck that makes a commit of them honest. This commit protects specifications; it does not ship code, and it claims no verification it did not run.

**Recorded as a departure under Captain's authority at sea, per the operator's instruction.** The doctrine gap it exposes: a voyage that never spends its watchbill has no custody event, so durable artifacts accumulate uncommitted for the length of the voyage. Worth raising upstream.

## QUIET PERIOD IN FORCE. Read this before writing anything.

**Operator agreed 2026-07-27: Captain writes no spec, scantling or asset edit from the next QM report until custody lands.** Everything the operator raises during it is written into this file and authored the moment the commit is in.

**Why, in one line: two standing rulings collide and this is the resolution.** Never split a voyage means the tree commits only when every target is green. An idle deck is worse than a wrong call means Captain dispatches and keeps editing while a role works. Both are right alone. Together they mean the deck hash moves under the role trying to establish a green, and run-record entries are hash-keyed, so each edit voids the evidence gathered before it. QM observed exactly this: `7251f42d` to `bd9a405` to `da80760` in one run, and reported that no whole-watchbill green can be established until the tree settles. Uncommitted work has been sitting since `5ba4996`.

**The quiet period is not a split and not an idle deck.** Roles keep working the whole time; only Captain's writes stop. It ends when Boatswain commits.

**State at the moment it began.** HEAD `5ba4996`, two commits ahead of `origin/main`, 42 files uncommitted, 25 watches and 111 targets, every reference resolving and gplint clean. Watches 1 through 15 were green at hash `da80760`; watches 16 through 25 are 36 undefined targets across six subsystems with no production seams: inference tagline and acronym generation, tagline arrival and spinner, accessible names, the credential setup form, tldr pages in the naming context, and tldr documented examples.

**A trap that fired twice today, so expect it a third time.** Editing a scenario's text strands its step definition. Renaming `at least the seven named commands` to `exactly the seven named commands` orphaned two definitions and Boatswain refused custody over it. Changing the literals `all five` and `all sixteen` to `all seven` and `all eighteen` did it again, and the step now reports "Step doesn't match any function". **Both counts are correct and verified against the checker, which expected 16 and found 18 with the delta being exactly the two new scantlings.** QM rebinds the definitions; `every step definition binds at least one scenario` catches it if QM forgets.

**Voyage 10 is sailing.** State as of 2026-07-27: HEAD `5ba4996`, two commits ahead of `origin/main` with no outbound run yet. `watchbill.json` carries 18 watches and 78 targets. QM has returned twice; watches 1 through 6 are green, watches 7 through 17 unworked, and only watches 1, 2 and 11 are committed. Everything else rides uncommitted as work in flight. QM's own static census counts roughly 110 unmatched step texts across the remaining watches, heaviest in `interactive-help`, `inspect-command` and `tom-inference`; that is a planning approximation and each focused run stays the authority.

## Outside review of the TOM concept, 2026-07-27. Commissioned by the operator, read-only, 600 words.

**Verdict: sound as a testing tool, oversold as a DOM equivalent, and the gap between those two is the concept's one real fault line.**

**Operator's correction to the review, 2026-07-27, and it overturns the headline objection. Read this before acting on anything below it.** The reviewer called the inverted causal arrow a weakness. It is the design. **HTML is prescriptive and the TOM is descriptive**: the DOM is the source of truth and pixels derive from it, while the TOM derives from pixels because the program is the source of truth and the screen is the only evidence a black-box observer has. **The TOM drives no TUI. It drives the tests for one.** Operator sharpening, and it closes the last reading: **not even Tinman's own assistant is driven by the TOM, its specs are merely specced with it.** `ratatui` draws the interface, the TOM observes what was drawn, and `assistant-ui.schema.json` attests the result conforms. The renderer never reads the schema; the schema checks the render. So there is no prescriptive direction anywhere in the system, not even where Tinman owns both sides. **Forbidden in advance: generating the assistant from `assistant-ui.schema.json`.** That inverts the arrow and manufactures precisely the coupling HTML cannot escape, and it will look like an elegant idea to somebody. So a presentation change being a semantic change is correct rather than brittle: a program that swaps reverse-video selection for a coloured bar has changed how it communicates state, and a test noticing is a test working. DOM-like stability would be actively wrong here, suppressing a real change and reporting green. A screen reader has the same property and nobody files it as a defect. **The reviewer measured the TOM against a contract it never signed.**

**What that leaves standing, and it is the useful part.** Name convention as convention rather than derivation; fix the "inferred" against "no inference is needed" contradiction; move to per-property style agreement; decide the name soft spot. **What it removes: any pursuit of DOM-like stability, such as an abstraction letting roles outlive a restyle.** That is the house of cards, and Captain came close to accepting it because an outside review arrived with authority. An outside review is input, not a verdict.

**The objection that lands, and it is causal.** In a browser structure is authored and presentation computed from it, so a role survives a restyle. In the TOM structure is recognized *from* presentation, so **a presentation change is a semantic change**: swapping reverse-video selection for a coloured bar shifts roles silently and binds locators wrong in the direction nobody notices, which is the failure the table Rule names for itself. It goes further: accessible names *are* raw screen text, so "roles and names rather than raw text" is half true, names being curated raw text, and a copy change breaks a name-locator exactly as it breaks a text match. This does not sink the tool, since testing-library rests on a computed accessibility tree too. **It means brittleness is relocated and reduced rather than eliminated, and the project's prose claims derivation where the truth is a maintained idiom catalogue.** Fix the prose; the design is fine.

**A contradiction in our own specs, caught by a stranger.** Scenarios describe the model as *inferred* while sitting under a Rule insisting no inference is needed. One of the two is wrong and it is the Rule: the grid genuinely determines cursor position and visibility, cell attributes, geometry and exit; it does not determine menu against list against decoration, which are decided conventions. **Sound if named as convention, currently narrated as derivation.**

**What it says we get right.** Borrowing ARIA rather than coining a taxonomy, with ARIA's constraints imported live, the name-required rule having caught a real fault the hour it was written. And the discriminator discipline: the prose-that-aligns negative control, and settling stream against screen from an observable fact rather than a flag.

**The concrete fix, and do it before more scantlings lean on style.** All-or-nothing computed style is defensible only where segmentation is fine enough that every uniform run is its own node, and it is not. **Per-property agreement is the honest form and what the DOM actually does**: a region whose cells agree on bold and disagree on foreground should report bold and omit foreground, rather than reporting nothing and forcing an author down to children that may not exist.

**The relief: the cannot-express-partial gap is not foundational.** A single frame genuinely cannot know a hundred-item list sits behind a ten-row pane, which is black-box observation rather than a modelling error, and screen-edge clipping is already expressible through `rect`. Granularity is not wrong at the root, so tables were the right place to start.

**The soft spot nobody had seen, and it needs a decision rather than a scenario.** The deterministic-roles and inference-names-only split protects roles, **but names are the primary locator key and names are the model-touchable part**, so a language model may touch exactly what tests grip hardest. A plan carrying a proposed name replays only where the deterministic builder reproduces that name. The role half of the split holds; the name half is where the risk actually lives.

**One tooling note, not conceptual.** JSON Schema failure locality will hurt authors: a failed recursive `anyOf` reports poorly. Worth knowing before authors meet it.

## Assistant layout, operator-stated 2026-07-27 from live use. Held for after custody.

**Confirmed by use rather than argued: the wait before launch is real.** The operator ran the assistant and it blocked on the acronym before drawing anything. That is watch18 precisely, so the watch is validated rather than revised.

**A layout contract, new, and coherent as one piece.** A pinned header carries the name and the acronym and never scrolls away. Content scrolls in the space between header and input box, clipped by the input box rather than pushing the header off the top. The scroll keys work **and are shown**. That last word carries the requirement: a region that scrolls without announcing it is a hidden affordance, the terminal's version of an invisible scrollbar, and the box already has a key-hints status region on its bottom border where they belong.

**The transcript appends at the bottom and follows automatically.** Operator, 2026-07-27. So the frame is header pinned at the top, input pinned at the bottom, transcript between them growing downward with the view following the newest exchange, and older content scrolling up under the header rather than pushing it away.

**Auto-follow must yield to the reader, and this is the detail that decides whether the scroll keys are usable at all.** Following while the view sits at the bottom is right; continuing to snap there while the operator has scrolled up to read an earlier answer makes the scroll keys useless and the interface hostile. Follow at the bottom, stop on scrolling away, resume on returning. It is one sentence to specify and the difference between a good interface and an annoying one.

**Full screen means height, clarified by the operator 2026-07-27, so nothing is superseded.** The box takes the full terminal height. `the box is no wider than the measure on a wide terminal` governs width, holds at 72 columns for readability, and is untouched: they are separate axes and only height was unspecified. Captain raised this as a possible conflict and it was not one, which is the right way round to have been wrong.

**The contract must attest the layout, operator 2026-07-27, and most of it already can.** `rect` carries `x`, `y`, `width` and `height` per region, so full terminal height is `root.rect.height == rows`, a pinned header is `y == 0` still holding after a scroll keypress, and the input's position is arithmetic on the same rect. `assistant-ui.schema.json` can constrain every one of those today with no model change. The shown scroll keys are attestable too, being text in a `status` region.

**ratatui carries the rest of the implementation and one gotcha is already closed.** 0.30.2 provides `Layout` constraints for the pinned frame, `scrollbar.rs` with `Scrollbar` and `ScrollbarState` so "shown" is a widget rather than hand-drawn, and `Paragraph::line_count(width)`, which is the wrapped-line count needed to clamp a scroll offset correctly and the papercut that usually makes ratatui scrolling wrong at the edges. What it does not provide is the follow-yields-to-reader state machine; that is roughly fifteen lines of owned state and nobody hands it over.

**A model-expressiveness question falls out, and it is the same one tables raise.** Asserting that the header stayed pinned while content scrolled is testable with what exists, since the header is either present after the keypress or it is not. Asserting that a region *is scrollable*, or that content is clipped rather than absent, is not: the model has no way to say a region continues beyond what is drawn. That is the intra-region granularity limit again, arriving from a third direction after tables and the underlined letters.

## What a plan is for. CORRECTED after third review, 2026-07-27. Read the correction first.

**Captain recorded a usage prediction as a definition, and it contradicts the corpus.** The operator said plans will likely be used for promptfoo-style automation and for driving a TUI into the state a real test cares about. That is a statement about expected use. Captain wrote it down as "a plan is navigation and synchronisation, not assertion", which the specs plainly refute: `harness-plan.schema.json` calls a step "one semantic action **or assertion**", defines `expect` as "**Assert** text is present on screen", and says a non-zero exit "is often **the behaviour under test**"; `replay.feature` opens with "a captured interaction becomes a repeatable test"; and `inspect-command.feature` makes the written plan "evidence they commit, re-run, and watch go red the day the program changes". **If `expect` were only a barrier the documented-examples probe would lose its entire product**, since documentation drift announcing itself as red is the thing it sells. **Plans are tests and carry assertions. The usage prediction stands; the redefinition is withdrawn.**

**The laundering risk did not vanish, it moved to locators, and nobody has looked there.** Three relocations the third review found. A **final** `expect` has nothing after it, so dropping or weakening it in migration is silent green. A **dropped** mid-plan barrier removes synchronisation and produces flakiness rather than red, which is also not loud. And **a locator rewritten to bind the wrong region makes the consumer's own assertion pass against the wrong thing**: the plan schema's `binding` rungs include `ordinal`, which is position rather than name, so weak locators are permitted by design, and a green replay cannot prove a locator rebound to the *same* region. **This is the open risk of the whole migration idea and it is unexamined.**

**So "do not build an assertion-preservation checker" stands only with its criterion stated.** Migration is a code change reviewed like any other, and that is a real criterion, but the review must check **assertion and locator equivalence** specifically, because green replay was agreed not to prove either. A cheap rewrite without that check is cheap in the direction of unverified.

**Spec defect found in passing, predating today: `replay.feature:8` promises a model property that does not exist.** Its Rule states the model schema carries which of a region's edges are anchored and which are elastic. `rect` carries `x`, `y`, `width` and `height`, and neither word appears anywhere in `tom.schema.json`. Verified by command. Either the Rule is stale or the model owes the property; one of them is wrong now.

**Naming contradiction to resolve: `test-command.feature` exists.** A command named `test` sits badly beside any framing where plans are not tests, which is a further reason the redefinition above was wrong.

## What a plan is for, as originally recorded and now superseded by the correction above.

**Tinman's own YAML is navigation and synchronisation, not assertion.** The operator expects two uses: automating promptfoo-style runs, and driving a TUI into the state the real test cares about. So a plan is the terminal's `goto` and `waitFor` rather than its `expect`, and the `expect` steps it carries are mostly barriers meaning proceed once this appears. A wrong barrier fails to reach the state, which is loud and immediate rather than a quietly weakened claim, which shrinks the migration laundering worry close to nothing.

**The driver protocol is therefore the primary interface and the plan is a convenience.** Assertions live in the consumer's suite, in their language, and always did.

**Where not to spend, stated so a future Captain does not drift into it: a rich plan-level assertion vocabulary.** That builds a second test framework beside the one every consumer already has. Both named uses want a plan that is good at arriving somewhere, not good at claiming things. Spend on reaching states reliably, on the driver protocol's surface, and on the observations a consumer's assertions need; leave claiming to them.

## Where the fiddliness actually goes. Operator framing, 2026-07-27.

**The hard parts are conceded rather than designed away, and that is the position.** Column shapes across `ls`, `top`, `df` and `git log`; migrating locators when the model moves; every idiom a rule set will not finish covering. These are fiddly and no cleverer rule set finishes them.

**What changes is that fiddly work is now cheap, and the combination is the product's actual advantage.** Inference proposes; a deterministic pass refuses anything the screen cannot back; coding agents do the mechanical rewriting; the assistant makes the whole thing reachable without a manual. **The mistake to avoid is treating fiddliness as a design flaw to be engineered out**, which produces the special-case treadmill and the speculative machinery the Articles forbid, rather than as ordinary work that now has good tools.

## Second review, 2026-07-27, testing the operator's corrections against the critic

**The reviewer conceded the arrow and kept two things, and the one it kept is sharper than the one it lost.** The descriptive defence covers changes in the program's drawing. **It does not cover changes in Tinman's own reading.** Column detection changed today, so a plan written yesterday against `list` output now meets `table` while the screen never moved; a red replay then means either the program changed or Tinman's reading changed, and the author cannot tell which. **That brittleness belongs to Tinman rather than to the program under test.** The answer is the pinning policy already set: a plan replays under the binary that wrote it, which is what the versioned npx invocation buys, so the two rulings turn out to depend on each other.

**Also conceded and worth holding: the framing proves the sensitivity is honest, not that it is proportionate.** Some presentation changes communicate and some are cosmetic, and the model reports both as semantic because it cannot tell them apart.

**The adoption answer, and it is a principle rather than a feature: assert only the communicative subset.** The scantlings already practise it, `assistant-ui.schema.json` bounding width rather than fixing it, requiring the status region without pinning depth, deferring colour. **Adoption survives while contracts keep that shape and dies if authors pin whole models.** Anything that encourages snapshotting a TOM wholesale is working against the tool.

**On the CDN `$id`: the stored problem is downstream.** Scantlings referenced across projects fragment by version, and no-deprecation means nobody downstream gets an overlap window. Acceptable while consumers are few; name it before they are many.

**On deleting `menu`: decide on evidence, not on a reviewer's remark or Captain's taste.** ARIA's `menu` carries interaction semantics `list` lacks, an active item and arrow navigation. If the grid offers distinguishing evidence, a highlighted item or cursor behaviour, it earns its place; if the only evidence is spacing, delete it. **The test is whether the deterministic pass can ever justify it.**

**On `top`'s merged columns: deriving the count from the header is principled, and for a reason already in the corpus.** The header is the program's own declaration of its columns, the source closest to the binary, which is the ranking rule `--help outranks a tldr page` states in another form. Geometry is the fallback where nothing is declared. **The honest cost is a headerless table mixing right- and left-aligned columns, which stays unreadable and should be reported as a limit rather than patched**; a third mechanism for that case is the special-case treadmill the review warned about.

## Inference proposes structure, reconstruction proves it. Operator 2026-07-27, and it amends a standing rule.

**Why a rule set cannot finish the job.** Column shapes are an unbounded idiom catalogue: `ls -la` carries no header, `top` carries one whose boundaries merge because a right-aligned column sits beside a left-aligned one, `df` differs again, `git log --oneline` has neither header nor alignment. Every mechanical rule so far has needed one more special case, which is exactly what the outside review warned the whole model risks becoming. **Inference generalises where rules accumulate exceptions.**

**The safety property is stronger here than for names, and that is the whole argument.** A proposed column layout is verifiable by **reconstruction**: split each row at the proposed boundaries, rejoin, and the original line must return byte for byte; every row must yield the same cell count; no cell may straddle a boundary. That is arithmetic over the character grid rather than judgment. The name rule's check is weaker, being only that the screen carries the text somewhere. **So a model proposing structure is better guarded than a model proposing names, which is the reverse of what the standing rule assumed.**

**Replay is untouched.** The plan records the boundaries and replay applies them mechanically, so no model runs at replay time and determinism holds exactly as before.

**This amends "the engine proposes names, never roles", and the amendment should be explicit rather than allowed to slide.** That rule existed to stop a model inventing structure nobody could check. Reconstruction is the check that did not exist when it was written. The amended form: **inference may propose any structure a deterministic pass can refute, and may never propose a structure it cannot.** Roles that no arithmetic can confirm stay the deterministic builder's alone.

## The direction this is going, operator-stated 2026-07-27. Discovery, nothing authored.

**Everything a TUI presents should be attestable: column count and order, key bindings, colours, layout. Inference builds the test case; the plan then runs deterministically.** That is an extension of the existing thesis rather than a new one, and the safety mechanism is already written: inference proposes, the deterministic pass refuses anything the screen does not independently carry, and replay invokes no model and no network. Naming already works this way. Assertions inherit it unchanged: a proposal the screen cannot back is refused rather than written, so a hallucinated assertion never reaches a plan and the plan the operator commits describes something that already happened.

**Two classes, needing different machinery, and conflating them will waste a voyage.**

- **Static attestations** are read from one screen: columns, colours, names, layout, measure. The only obstacle is model expressiveness. **Nothing can be attested that the model cannot express, so the model is the gate on this entire direction, not inference.** Colour landed on voyage 10. Tables have not.
- **Behavioural attestations** are not on the screen at all. What `q` does, whether `?` opens help, how navigation moves: these cannot be read, only **pressed and observed**. That is a recorded flow and `record` owns the seam.

**The idea the operator's question implies, and it is the sharpest available next move: inference-driven exploration.** Tinman already probes a program's documentation, runs the examples inside the sandbox, and writes a plan carrying only what was honoured. Point the same machinery at the interface rather than the documentation: try the keys, observe what changed, write down what held. The isolation that makes running a stranger's documented command safe is exactly what makes poking an unknown TUI safe, so the two are one capability with two sources of hypotheses. The operator noted a single inspect cannot settle it, which is right: a keypress needs a session, and driving sessions is what the product is.

## Held for after custody: the model cannot express a table, and a real program proved it

**Operator ran `tinman inspect top` the hour terminfo landed and asked how a test would use the output.** It produced `menu` holding a `menuitem` per whitespace-delimited word: `"top"`, `"-"`, `"15:27:43"`, `"up"`, `"days,"`. Wrong twice: a header line is not a menu, and words are not items. It is worse than raw text, because it looks addressable and is not.

**What the operator wanted to assert is exactly what the screen carries.** `top` draws a summary block of labelled values, `13447.6 avail Mem` among them, and a table under a `PID USER ... %MEM TIME+ COMMAND` header row. "Available memory is over 10G" is a labelled value; "claude and opencode are running" is a row whose `COMMAND` cell holds each name. Neither is expressible: the role enum is `application, region, menu, menuitem, list, listitem, button, textbox, status, log, article` and carries no `table`, `row`, `columnheader` or `cell`.

**The gap is a stream gap before it is a screen gap, and that half matters more.** Operator, 2026-07-27: even `ls -la` needs columns to test. Today it yields one `listitem` per line whose name is the entire line, `"-rw-r--r--  1 1000 1000    669 Jul 25 14:51 .env"`, so asserting a size or counting directories means substring-matching that string. **Watch11's own Rule names that exact failure as the thing it exists to prevent**, "the test author is back to matching raw text", and the rule avoids it for bare `ls` and walks into it for `ls -la`. `ps`, `df` and `git log --oneline` are the same shape. The standing preference that Tinman stay useful for plain command-line testing makes this the more valuable half of the work, not the TUI half.

**Two column shapes, genuinely different.** `ps`, `df` and `top` carry a header row, so columns have names and a test addresses `COMMAND` or `%MEM`. `ls -la` carries none, so columns are positional; WAI-ARIA permits a table without column headers, so cells are addressed by index and the model invents no name it cannot see.

**The discriminator needs the same care the multi-line-block rule got.** Alignment must mean columns sustained across rows rather than coincidental spacing in prose, or a paragraph becomes a table and the reading is wrong in the direction nobody notices.

**The shape to author: derive a table where aligned columns sit under a header row, addressed by column name, and a status region carrying labelled values for a summary block.** WAI-ARIA already names every one of those roles, so this is the vocabulary the model borrows rather than a new one. `table` was struck from the enum earlier this voyage, correctly, because the skill named roles the model had never produced and a role is added when a scenario needs it. **A scenario needs one now.**

**Also observed and unexplained.** `tinman inspect 'top -b -n1'` reports "the program did not start" while plain `top` renders, and batch mode runs fine outside the sandbox. Either the curated terminfo set is thinner than batch `top` needs or the new start-detection reports a false negative. Both matter and neither is guessable; run it before writing anything.

**Stream shape and screen shape are different readings, and this is the first live full-screen program the project has looked at.** Watch11's rules read a program that exits. A program still running is a screen, and its idioms are tables and status lines rather than lists and logs.

## Cold read of src quality, 2026-07-27, and what was authored against it

**Measured, not opined.** 5,899 lines over 19 modules, largest `tom.rs` at 1,274, no god file. 1,629 doc lines against 5,899 code, roughly 28 percent, explaining why rather than restating signatures. Zero `TODO`, `FIXME`, `HACK`, `XXX`. 40 `.clone()`. Clippy clean at `-D warnings`. Structure and documentation are genuinely strong.

**The weakness is that it panics where it should return errors: 79 sites.** `src/driver.rs` carries 25, and that module speaks JSON-RPC 2.0 to a caller in another language over a protocol that has an error object, so an abort hands the caller a dead pipe on the surface every external integration depends on. `src/assistant.rs` carries 17 and has **no panic hook**: line 391 disables raw mode on the ordinary path only, so an abort mid-session leaves the operator on the alternate screen in raw mode with no prompt. `src/flow.rs:155` is a live `todo!()` on `Action::Activate` and `Action::Fill` inside the TUI step loop, so a plan using either crashes `tinman test`.

**Security reads clean where it matters.** The credential is formatted in exactly one place, `format!("Bearer {key}")` into the auth header, and reaches no output. Five sites build `sh -c` from input, which is what the product does and the sandbox is the mitigation; the rule to hold is that this is only safe inside the sandbox.

**Performance has no measurement at all.** Every deadline is a hand-picked constant and one was wrong by a factor of eight.

**Authored against it with the scantling-first shape the operator asked for: three scantlings, five scenarios, where the behavioural route needed ten-plus.** `bwrap-isolation-policy.json` gained `forbid.credentialFileInWorkspace` at zero scenario cost, because its `@contract` scenario already discharges the file. `panic-free-seams.json` forbids the aborting constructs across the two seams that must answer instead, which needed the reference-boundary branch to accept `searchPaths` rather than a single `module`, since per-module contracts have already failed this project once. `latency-budgets.json` declares seam and ceiling as a list so a new timed seam adds a line rather than a scenario. The five scenarios carry only what a prohibition cannot prove: an error object returned, a terminal given back, a plan with `Activate` and `Fill` replaying.

**The division that made it work, worth reusing: scantlings for what must never appear, scenarios for what must happen.** A forbidden-construct list is exhaustive over a tree and a scenario cannot be; a returned error object is an outcome and a scantling cannot see it.

## Three blockers closed 2026-07-27, and only one of them needed the operator

**The accessible-name subset is WAI-ARIA's, not ours.** QM could not encode it because no durable artifact named which roles require a name; the specification names them. Required: `application`, `region`, `menuitem`, `button`, `textbox`. Not required: `menu`, `list`, `listitem`, `status`, `log`, `article`. Encoded as an `allOf` conditional in `scantlings/tom.schema.json` and **proven to fire** against a validator, seven cases including the negatives: a `button` with no name is invalid, a `list` with no name is valid, an empty name is invalid. The roles left out are the specification's judgment rather than an oversight, which the schema's own description records so nobody "fixes" it later.

**The bwrap scantling was Captain's defect and is scoped now.** `workspaceMount` demanded `--overlay-src` and `--tmp-overlay` on every generated argv, but two workspace modes are legitimate: the operator's own directory, which must survive untouched, and a workspace Tinman created for the caller, whose host-visible writes `features/flow-orchestration.feature` pins in three scenarios. A flat invariant condemned the second. `appliesTo: operatorWorkingDirectory` names the subject, the meta-schema requires the key, and the policy validates.

**The tldr question was asked, answered, and then dissolved by the operator, 2026-07-27.** Captain framed it as how to get a raw page out of the operator's installed client, and put three options up. The operator's reply was that declining to be a *client* never meant declining a *request*. Ruling: **fetch the page as raw markdown from the tldr project directly.** Verified by command: `pages/common/printf.md` answers 200, `pages/linux/ls.md` answers 404 while `pages/common/ls.md` answers 200, so platform resolution is two attempts and no cache. The markdown arrives with `{{[-a|--all]}}` intact, which is the form the style guide specifies.

What it removes is the point. No client need be installed, no `--raw` extension need exist, and the whole question Captain had just escalated stops existing. **That is the third time today the better answer dissolved a tradeoff rather than picking a side**, after the workspace overlay and the placeholder expansion. Captain's error each time was designing within the options it had listed instead of asking what would remove them.

What stays true: Tinman implements no client specification, keeps no cache, and credits CC-BY-4.0 in the plan. Nineteen scenario Givens across `tom-inference.feature` and `inspect-command.feature` were rewritten from "the operator's tldr client has a page" to "the tldr project has a page", and the Rule arguing the old design was rewritten with them; nothing in the specs names an installed client now.

**Pivot: `dependency: tlrc` in `RIGGING.md` is now unused by the product.** It was installed to be the client Tinman asked. Strike it once QM confirms verification does not want it for staging a page; leaving it recorded and unused is the drift the dependency policy exists to prevent.

## The tagline raises its own acronym, watch22

**The model proposes and a deterministic pass decides, which is the naming doctrine applied to Tinman's own copy.** An expansion is a claim that the words spell the name. The pass walks it for the letters of "tinman" in order and raises them, which proves the acronym and shows the reader where it hides; an expansion with no such sequence is not an acronym and is asked for again, bounded by the same tagline ceiling so a model that keeps missing cannot spin.

**Settled 2026-07-27: a raised letter mid-word is accepted, so the fallback pass stands.** The operator's words, on seeing `AssertioN`: it could have capped the n in Assertion. So the two-pass shape is confirmed rather than tightened: prefer letters that begin words, fall back to any position, and accept the result. Requiring every letter to begin a word was the alternative and it was declined; it looks cleaner and spends retries against a ceiling to buy it. Extra trailing words are fine too, so an expansion whose acronym completes before its last word is valid.

**The TINMAF episode, kept because Captain got it wrong in an instructive way.** The live assistant produced "Terminal Interaction and Non-repudiable Model-free Assertion Framework". Captain read the initial of every significant word, got TINMAF, and reported the model had failed. It had not: the closing `n` is the last letter of `Assertion`, and the algorithm finds `Terminal Interaction and Non-repudiable Model-free AssertioN Framework` on its fallback pass. **Captain applied a rule the specification does not contain, then judged the output against it.** The check exists for expansions that genuinely do not spell the name; this was not one, and a word-initial-only reading would have thrown away a good answer. Read the letters in order, not the initials.

**The operator's draft carried a real bug and the scenario caught it before Crew saw it.** Judging word-initiality against the moving tail rather than the whole string makes index 0 of the tail look like a word start, so the run took the `n` inside "interaction" and produced `Terminal INteraction and networkless ...`. Measured, not reasoned: the corrected form yields `Terminal Interaction and Networkless Model-driven Application Navigator`. **The scenario pins that string, so it reddens against the draft.** Specifying the result rather than the method is what made the spec outlive a wrong implementation.

**Efficiency is not a consideration here and should not become one.** Six letters over roughly seventy characters, once per help render. The available win is simplicity: two plain scans, word-initial first and any-position as fallback, rather than a candidate list per letter. That also reads better, since all six letters begin words or none of the preference applies, instead of a mixed result that looks like a mistake. This paragraph is guidance, not a requirement; implementation is Crew's.

## The tagline never filled, and every fix Captain proposed was a symptom

**Measured, 2026-07-27, against the configured provider.** The acronym request as composed, the asset prompt plus the skill name and description, returned nothing inside **forty seconds**, twice. With `reasoning: {enabled: false}` the same request answers in **1198ms and 2416ms** and produces "Terminal Interaction and Networkless Model-driven Application Navigator". `max_tokens: 24` is a trap: it truncates mid-phrase to "Tinman Is Not a Model And Not". So the fix is the request, and neither the prompt nor the ceiling needed touching.

**Captain proposed three fixes and all three were symptom management**: raise the 5s `TAGLINE_CEILING`, cache the result in the wake, or drop the tagline. The operator asked why a six-word acronym takes five seconds, and then named the cause. **A budget raised to fit a call that should never have been slow hides the cause and pays the latency for ever.** Reach for the request before the ceiling.

**Why nothing caught it.** The tagline is decoration, so its absence renders as a design choice rather than a failure: help without a tagline looks deliberate. Three scenarios assert tagline content and passed against the unavailable path. Watch20 closes it from both sides, a request-shape scenario that needs no credential and an `@inference` scenario that times the real call against the ceiling.

**Open, the operator's suggestion: render help immediately and fill the tagline asynchronously behind a spinner.** At 1 to 2.4 seconds this is polish rather than a fix, and it only applies on a terminal, since redirected help renders once and exits. Worth doing if `--help` should be instant unconditionally; not needed to make the tagline work. Decide before watch20 reaches Crew.

**Custody was refused on a foul Captain caused, 2026-07-27, and the lesson is about the channel rather than the edit.** Rewriting `the parser accepts at least the seven named commands` into the exact form, and deleting the duplicate exact-set scenario from `command-dispatch.feature`, stranded two step definitions: `the parser accepts every one` and `it is exactly {string}, {string}, {string}, {string} and {string}`, both in `tests/cucumber.rs`. `features/methodology-conformance.feature:every step definition binds at least one scenario` caught it exactly as designed. **It sat on no watch, so QM would never have been dispatched to it.** A derived check with no watchbill entry discovers the fault and cannot cause the fix; the check and the channel are two things and only one of them existed. Resolved by listing the conformance target at the head of watch7, the next unspent watch, so it is worked before anything else. **Editing a scenario's steps strands its step definitions; list the orphan check with the edit, the same way a new scenario is listed with its watch.**

**Ruling: the unconsumed `tui-markdown` rides this commit.** Boatswain correctly flagged that nothing in the tree references it and that watch8 is the consumer, so the install landed ahead of its seam. Holding it out would cost a Shipwright redispatch later to add a line that is already recorded, installed and building. It is ordered by a watch in this voyage, which makes it work in flight rather than dead weight.

**Two verifications Boatswain labelled unverified, answered by Captain.** `tldr --version` reports `tlrc v1.13.1` and `bwrap --version` reports 0.11.2, so both newly recorded dependencies are present. The next custody pass still owes `broad-sandbox`: the `TerminalSession::start` to `spawn_on_pty` refactor is verification support, whose blast radius is the whole tier rather than a focused set, and a fresh Boatswain re-derives that from the diff.

**Shipwright cleared the rigging round trip, verified by Captain: `tldr --version` reports `tlrc v1.13.1`, `Cargo.toml:25` carries `default-features = false`, and `syntect` and `ansi-to-tui` are both absent from `Cargo.lock`.** Its finding worth keeping: `## Dependencies` has twice run ahead of the tree this voyage, because Captain records a selection in the same pass it is decided while installation is a separate role's write. That gap is the design working rather than failing, but it means a recorded dependency is a claim until a build answers.

**The tldr ambient-cache risk is closed by the specs themselves, and the reason generalizes.** Shipwright flagged that the page cache lives in the operator's home, outside the repo, so a `Given` naming "the operator's tldr client has a page for X" could lean on whatever happens to be cached. Checked: the real `tldr printf` page carries no `printf hello`, so every one of those scenarios names example text no ambient page supplies and cannot be satisfied without provisioning. **Concrete fixture data in a `Given` is what makes an ambient-state precondition falsifiable.** A vaguer wording such as "a tldr page exists for printf" would have passed on the operator's cache and taught us nothing.

**`tlrc` stays unpinned, deliberately.** `bwrap >= 0.11.2` carries a constraint because a scenario needs `--tmp-overlay`; no scenario names a tlrc version, and the policy pins only where a scenario or the dependency policy requires it. Bare is correct here, not an omission.

**QM's four blockers are resolved, 2026-07-27, each verified by Captain before acting.**

1. **Contradictory intent, and the contradiction was inside one Rule.** `command-dispatch.feature` asserted the parser accepts exactly five commands while `command-surface.feature` named seven as a floor. Its own Rule already demanded exactness, "a command added to the parser without being named here reddens rather than shipping", which a floor cannot deliver. Resolved by naming the set once: the exact-set scenario is struck from `command-dispatch.feature`, whose Rule now points at the single home and records the drift, and `command-surface.feature` carries the one assertion, now exact rather than a floor and renamed accordingly. The watchbill reference was repointed in the same pass, because a renamed scenario leaves a focused run selecting zero scenarios and exiting green.
2. **Rigging: no tldr client installed.** Watches 10 and 12 through 15 need one. `tlrc` 1.13.1 selected, the official client and Rust-native so it installs through the toolchain already present. Recorded under `## Dependencies`; install routes to Shipwright.
3. **Missing intent: the bare-terminal path had no credential guard.** Every existing bare-tinman scenario opens `Given inference is available`, so nothing said what happens without a credential. New scenario mirrors the redirect pair.
4. **A shipping defect, and QM found it by running the binary.** The emitted man page's DESCRIPTION printed `@planks("each is passed to the command parser")` at the operator, because clap_mangen derives the page from the doc comments where the trace annotations live. That is the one artifact a packager installs system-wide. New scenario pins it, carrying a positive assertion beside the absence one so it cannot pass on empty output.

**One change waits on QM's report: Shipwright applies `default-features = false` to `tui-markdown`.** Recorded under `## Dependencies` as the selection. `Cargo.toml` is Shipwright's write and the change waits so two roles never share the cargo lock. Confirm with `cargo check` afterwards, because a feature flipped off is a claim until the build answers. Why: the default `highlight-code` pulls `syntect` and `onig_sys`, a C library needing a C toolchain, which taxes `cargo install tinman` on a clean machine and weighs on `@sandbox`. Watch8 asks that a fenced block be rendered and set apart, which is block layout rather than syntax colouring, so the specs hold either way.

**Watches 14 to 17 were authored mid-voyage from operator conversation, after QM had sailed.** A QM holding an older copy of the watchbill reports against the count it read, which already happened once and is the expected recovery rather than a fault: the watchbill is not spent and the next QM takes the remainder. All 75 references were verified by extraction at write time, and `npx --no-install gplint "features/**"` exits 0.

**Expect these to be the expensive watches.** Watches 10 through 13 specify a capability that exists in no form yet: reading documentation, probing it in the sandbox, and writing the result as a plan. A blocker from those is expected rather than surprising, and the operator chose to sail them in this voyage rather than split.

**Captain runs in the main loop. Operator preference, stated 2026-07-27.** Captain is the only human-facing role and outbound may run only in the human-facing session, so Captain keeps the operator's seat and the internal roles are dispatched away from it. Dispatch QM as a context-isolated subagent rather than clearing this session and becoming QM: that satisfies the bulkhead, keeps Captain on deck, and is the route doctrine prefers wherever the runtime supports it. **Captain never assumes QM.** Dispatch thin, per the contract: role and base commit, nothing else. The watchbill at its home is the channel and the durable artifacts are the hand-off.

**What Captain owes while QM is away.** Dispatching into isolation hides the work, so narrate it: plain prose, at least every couple of minutes, on what is running, what it has produced and what it is waiting on. Read QM's progress to narrate, never to decide whether it has finished. The report is the only signal that says finished, and a QM reading as stalled on a quiet tool result has twice been caught mid-`git diff` and reported seconds later.

**Then, in order.** When QM reports the watchbill spent and its targets green, dispatch Boatswain for post-implementation custody, carrying the job, the base commit and QM's advanced target references. When Boatswain reports passing verification, a clean tree and a local commit, summarize and offer outbound. Do not order a full regression: it is a harbour action and harbour is its only trigger, so offer harbour where a whole sweep is wanted. The crate ships with `cargo publish` and verifies with `cargo search tinman`, both from `## Outbound` in `RIGGING.md`.

**Expect these to be the expensive watches.** Watches 10 through 13 specify a capability that exists in no form yet: reading documentation, probing it in the sandbox, and writing the result as a plan. A blocker from those is expected rather than surprising, and the user has already chosen to sail them in this voyage rather than split.

## What actually binds Captain. User-confirmed, 2026-07-26.

Two things, and they are absolute. Nothing from conversation reaches QM outside durable artifacts. Captain never writes production code outside the Perturbation policy's named exception.

Everything else is a default, not a wall: write scopes, which role owns which file, and reading anything at all. Captain departs from a default where following it would stall the voyage or leave a fault standing, does the work, and records the reason in one sentence. **Do not work as though under-permissioned.**

**Read freely.** Transcripts, source, a dispatched role's progress. One trap, learned by falling into it: do not infer *doneness* from a transcript or a process table. The report is the only signal that says finished. Read to narrate, never to decide completion.

**Keep routing writes to the owning role, for the right reason.** Not permission, verification. QM owning the step definitions is what caught an assertion passing on containment and a colour join comparing a 60-row grid against a 24-row rectangle. That is worth a round trip. It stops being worth it the moment it costs a cycle to avoid a one-line edit.

**Do not evade doctrine on `Rule:` usage.** User correction, 2026-07-27. A `Rule:` carries durable context only: the why, the trap, the history. The requirement lives in the scenario and nowhere else. Writing a `Rule:` as a normative headline with rationale attached is the evasion, and the corpus has old examples of it that are not licence. Every `Rule:` authored on voyage 10 was rewritten once for exactly this.

## Voyage 10, authored 2026-07-27. What it carries and why.

**The security defect is the reason it exists.** `record` and `inspect` both launched their target with the operator's real home, environment, PATH and network. Confirmed by command: `grep -rn "PreparedProcess {" src/` returns `src/record.rs:196`, `src/inspect.rs:20` and `src/bwrap.rs:186`, and only the last is legitimate. `src/backend.rs:40` already carries `resolve(requested, allow_unsafe)`; neither command calls it, so the machinery was built and never wired. User ruling: **Tinman never executes a CLI or TUI outside the sandbox, for any command.**

The fix is structural: `scantlings/prepared-process-construction-boundary.json`, a new **construction-boundary branch** in the proof-contract meta-schema, bounding construction of one named type over a whole search tree. A per-module reference contract covers the modules someone remembered; the modules that bypassed the backend were exactly the ones nobody had.

**Consequence to expect:** every `record` and `inspect` scenario that launches now needs bwrap, so they moved to `@sandbox`. That tier gains roughly ten scenarios. Observed floor was 26 to 34s at 36 scenarios against a 120s budget, so headroom is fine, but the budget check is live and will say so if not.

**The TOM grew a stylesheet.** User question, and it reframed the voyage: if this were a webapp we would have HTML and CSS in assets, so what is the Tinman equivalent? Answer: the TOM tree is the HTML and it already existed; the CSS is computed style per node, which the model was throwing away. `alacritty_terminal` hands Tinman an attributed cell grid, the builder reads those attributes to derive roles, and then discarded them. So `tom.schema.json` gained `style` per region and `cursor` on the model, and `assistant-ui.schema.json` stopped confessing that colour and cursor are properties the model cannot carry. It is also a product capability that was missing outright: a test author could not assert *the error line is red*.

**Style is absent where a region's cells disagree.** That is deliberate: one computed style for a region drawn two ways is a summary, and asserting against a summary is asserting against Tinman. Absence means mixed, not plain; plain reports the defaults explicitly, so the shape is uniform.

**The assistant.** Alternate screen for the session with the transcript written into scrollback on exit; one 72-column measure shared by the box and the transcript; the question marked by a background block running the full measure, with a leading `> ` marker as the NO_COLOR fallback; markdown rendered through `tui-markdown`; the name and tagline coloured apart from the help body; bare `tinman` opens the assistant on four conditions. The `COMMAND: ` marker is now taught by the instruction asset and joined to the parser by a scenario, and the last marked line is the proposal with the prose above it the answer.

**Discoverability, per the standing direction that there is one surface and three consumers.** `tinman man` and `tinman completions <shell>` emit at runtime rather than shipping generated files. The seven-command floor is named. The skill-to-parser check widened into one sweep over every shipped markdown file, and three new joins stop the skill drifting again.

**The skill was lying in two places and nothing reddened.** It named nine roles the model has never produced, including `table`, `dialog`, `statusbar`, `message-pane` and `treeitem`, and its driver examples used an `op` key and a top-level `session` where the protocol has been JSON-RPC 2.0 with `method` and `params` for two voyages. Both fixed, both now checked. Verified after the fix: every fenced example in `SKILL.md` validates against the scantling it illustrates, and no role token it names is outside the model's enum.

## Watch14, authored 2026-07-27 from the tldr-pages research

Read [the style guide](https://github.com/tldr-pages/tldr/blob/main/contributing-guides/style-guide.md) and [CONTRIBUTING](https://github.com/tldr-pages/tldr/blob/main/CONTRIBUTING.md) before touching any of this again. Both were fetched, not recalled.

**A tldr example is a template, and watch12 assumed it was a command line.** The syntax is fixed by the style guide: `{{path/to/file}}`, `{{[-o|--output]}}` to let a client choose short or long form, `{{option1|option2}}`, `{{1..5}}`. Every watch12 scenario used a placeholder-free example, so nothing said what happens to `cp {{path/to/source}} {{path/to/target}}`. Run as written it is refused, and inspection then reports an unhonoured claim it manufactured itself. **User ruling: substitute what Tinman can safely fill, report the rest as untestable as written.** A path placeholder becomes a real fixture inside the sandbox; the plan records the line Tinman ran and the example it came from as two separate facts, so the page is never credited with words Tinman assembled.

**Credit was covered for naming and missing for examples.** Watch10 credits the project when a page informed naming; nothing credited a page that supplied an example. Two scenarios close it, mirroring watch10's shape, floor control included.

**mandoc is installed and recorded.** `man emits a roff man page` asserted a title macro and the command names, which malformed roff satisfies. `mandoc` is the language's own parser and the independent tool the standing preference asks for, so the `@contract` scenario runs it. Installed by apt on this VM under the Blocker policy's environment-and-tooling row, recorded under `## Dependencies`.

**The severity threshold is the whole trap, and the scenario says "error" for that reason.** Measured on this VM, true exit codes read without a pipe: a well-formed page exits **2** under a bare `mandoc -T lint`, because a missing date and a lower-case title are a WARNING and a STYLE. Reading that as failure would redden a page that is fine. Under `-W error` the same page exits **0** and a page with an unknown macro exits **3**. So `-W error` is the threshold, the check is falsifiable, and it does not fire on cosmetics. Measuring it also caught the pipeline-exit trap live: `mandoc ... | head; echo $?` reports `head`'s status and reads 0 for a page mandoc rejected. Use `PIPESTATUS` or no pipe.

## Watch15, authored the same day, and a correction worth keeping

**The keypress notation is watch15.** tldr pages carry `<Ctrl c>`, `<Esc><u>`, `<ArrowUp>` in a documented syntax, so a page for a full-screen program documents the interactions and not only the invocation. That is the half of a TUI's documentation no shell can test, and it is a plan step in a notation somebody else already standardized. Two scenarios: a recognised keypress becomes a key step that replays, and an unrecognised one is reported untestable as written with the plan-not-empty floor. Sending it as text would type literal angle brackets into the program, which is the placeholder fault wearing a different hat.

**The house-style pass landed on `assets/help/tinman.txt`.** Checked against their guide rather than assumed: the command descriptions were already imperative, already unstyled, already using `stdin`/`stdout`, and already ordered with `help` last. The one real gap was that the help text carried **no examples at all**. Four now, simplest first and progressively more complex, with a scenario passing every one through the parser. That direction was genuinely unguarded: the Commands block is checked both ways, and an example line escapes both checks because it carries a subcommand, flags and arguments together in the one form the parser actually receives. `SKILL.md` and the man page are not in this pass: the man page is generated from clap doc comments, which is production code and not Captain's, and `SKILL.md` is bound by three bundled-skill scenarios that make it a bigger and separate job.

**Correction, and the reason to keep it.** Captain recommended holding both on the grounds that rewriting the help asset would redden a scenario mid-flight. That was wrong and one grep settled it: `src/help.rs:10` inlines the asset with `include_str!`, so a run in flight uses the compiled binary and an edit lands at the next rebuild. The real constraint is narrow, keep the Commands block, the Options block and the single `{{tagline}}` intact, and it is checkable. Verified after the edit: 1 tagline, 7 commands, 2 option lines, 4 example lines all parsing as Tinman commands. **The recommendation to hold was Captain managing its own uncertainty rather than a real risk, and the user was right to push on it.**

**The README install idiom is prose, not a fenced block, and that was deliberate.** `every Tinman command line in shipped markdown is accepted by the parser` sweeps README fences, and `tinman man > /usr/local/share/man/man1/tinman.1 && mandb` is a shell line rather than a Tinman command line. Prose keeps it out of the sweep. If a later voyage wants it checked, the sweep's extractor is what needs the scenario, not the README.

## Watch16: the sandbox let a probed program rewrite the operator's tree

**Found by the operator, not by us.** `tinman inspect "touch TEST"` left a `TEST` file in the repository root. Reproduced and diagnosed by command: `src/main.rs:69` reads `std::env::current_dir()`, `src/inspect.rs` hands it to the backend as the sandbox home, and `src/bwrap.rs:95` binds it **writable** at `/sandbox` and chdirs there. `record` and `test` do the same at lines 51 and 62.

**Why the whole feature stayed green.** `inspect cannot write outside the sandbox` asserts a sentinel path *outside* the sandbox survives. The operator's working directory is *inside* it. The scenario is correct and its name reads like a guarantee it never made. Meanwhile the feature's own `Rule:` already promised "an example that destroys its working tree destroys a copy nobody will miss" while the code bound the original. **Durable context and behaviour contradicted each other and nothing reddened**, which is the case the notes say to answer by writing the scenario that reddens.

**The operator proposed the fix and it beat all three options Captain offered.** Overlay, the way podman and docker do it: `--overlay-src <dir> --tmp-overlay /sandbox`. Verified live on this VM, not recalled: inside the sandbox the program read the real file, created `TEST`, overwrote `notes.txt` and read back the overwrite; on the host afterwards `TEST` never existed and `notes.txt` was unchanged. It beats read-only-plus-scratch because programs that legitimately write keep working, beats a writable copy because there is no copy cost and `writable_copy_of` was a shallow top-level-files copy that silently skipped directories, and beats an empty workspace because the program still sees the real project. Replay gains determinism for free: every run starts from a tree no earlier run could have changed.

**Version floor `bwrap >= 0.11.2`.** 0.11.0 introduced the overlay options, released 2024-10-30, read from the project's own NEWS. The operator pinned .2 on the argument that a floor nobody tests is a claim, which is the same reasoning as the adoption-proof gap below. No kernel floor is named and none should be guessed: the launch attempts the overlay and reports what the kernel said, because a pre-check is the credential-probing antipattern wearing different clothes.

**Where it is specified.** No new scantling and no new checker: `bwrap-isolation-policy.json` gains `workspaceMount` requiring the `--overlay-src` and `--tmp-overlay` pair and `forbid.operatorWorkingDirectoryAsBindSource`, and `proof-contract.schema.json` gains both keys so the policy still validates. The existing `features/bwrap-argument-generation.feature` attestation discharges it, and the existing construction boundary already forces every prepared process through `src/bwrap.rs`, so one module decides workspace treatment for all three commands. That is the centralized lodge the operator asked for; the sprawl was never in where the decision is made, it was that three commands each read `current_dir()` and handed it in.

**A scenario Captain proposed and then dropped, with the reason.** A runtime "overlay unavailable is a hard failure" scenario would need a condition the real environment cannot produce on demand, so it would arrive as a justified double. The static prohibition already forbids binding the working directory writable, so a fallback to a writable bind cannot be written without reddening the attestation. Machinery declined, not deferred.

## Standing preferences the user has stated

Read these before authoring any spec.

- **Whenever the consumer is a human at a terminal, it should be as beautiful as the land of Oz.** An exemplary modern TUI, to the highest usability and accessibility standards, and always sandboxed. User-confirmed 2026-07-27. Tinman reads terminal programs for structure, naming and presentation, so a Tinman interface without them argues against the product. This is falsifiable rather than a matter of taste, and the way to keep it falsifiable is the TOM: Tinman's own screen is checked by the instrument Tinman sells.
- **The accessibility floor that a terminal program can actually break is meaning carried in colour alone.** Contrast belongs to the theme and wording to the copy. Every distinction drawn in colour needs a second carrier, and NO_COLOR is the test.
- **Styling reaches a terminal and no other stream.** A colour escape in a pipe is corruption of somebody's data.
- **A convention that looks like it does not fit is usually one whose adaptation has not been found yet.** Operator, 2026-07-27, sharpening the rule below. A practice a modern project follows generally encodes a reason somebody learned expensively, so the burden of proof sits on the objection rather than on the adoption. **When a convention appears not to transfer, ask what would make it transfer before concluding it does not.** Captain twice ruled that tldr-pages placeholder syntax could not apply to `--help`, with three specific objections, all of which dissolved the moment the operator proposed expanding placeholders at render time, using the mechanism `{{tagline}}` already established in that same asset. The objections were sound against a naive adoption and Captain stopped there instead of designing the adaptation. Reasoning about why a practice fails here is the easy half; the useful half is finding the shape in which it succeeds.
- **The TOM must never become HTML. Operator, 2026-07-27, and this is the stronger form of the ruling below.** HTML's disease was not backwards compatibility, it was losing the ability to delete: every idea that ever shipped is still in there, so the language grew five ways to do everything and quirks mode to adjudicate between them. **Old ideas are deleted, new ideas are implemented, and there is no wiggle room for cruft ever.** No aliases, no legacy fields, no dual support, no deprecation window. The TOM can do this because nothing negotiates at runtime: the version is in the `$id` and available as `npx @dk/tinman@x.x.x` for anyone who wants it, so a removal costs nobody anything. **The consequence to hold onto: with no deprecation path, an addition is a commitment escapable only by deleting.** That makes deletion routine rather than traumatic, and additions expensive, which is what "a role is added when a scenario needs it" was already protecting. Prune the model as readily as it grows, and treat a role or attribute nothing asserts as a defect rather than as harmless.
- **Migration to the latest model is machine work, not a person's. Operator, 2026-07-27, and it makes deleting safer than deprecating.** The usual objection to free deletion is that consumers are left hand-editing their plans and scantlings. They are not: a migration is a mechanical transform over documents Tinman already models, and **inference proposes the rewrite while the deterministic pass proves it, because a migrated plan can simply be run.** A plan that replays green against the new model is a migration proven correct; a person hand-reconciling two model versions produces a claim nobody checked. **A migration that does not verify is reported rather than written**, which is the documented-examples rule exactly, where only an honoured example earns an expectation. **Correction, second review 2026-07-27: a green replay is necessary and not sufficient, and Captain recorded it as sufficient.** Green proves the migrated plan passes, not that it asserts the same things; a model that drops an expectation or loosens a bound replays green and launders coverage away silently. **Operator correction, same day, and it scopes the reviewer's concern down to where it actually applies.** **Assertions live in the consumer's own suite.** Somebody driving Tinman from pytest or cucumber-js writes expectations in pytest or cucumber-js, and the TOM is what makes those expectations provable against a terminal. A model change touches how they address the screen, the roles, names and locators, never what they assert, because their assertions were never Tinman's to rewrite. The laundering risk therefore applies only to Tinman's own YAML plans, where `expect` steps do live in the artifact, and that is one consumer shape rather than the main one. **And there, a migration is a code change and code changes get reviewed**: an agent rewrites the locators, a human reads the diff, the suite runs. Building an assertion-equivalence verifier for it would be machinery for a problem review already solves, and speculative apparatus is what the Articles forbid. **No assertion-preservation checker is owed. Do not build one** - but see the correction in the plan section: the review that replaces it must check assertion and locator equivalence explicitly, because green replay proves neither. So the version story is complete in both directions: stand still by pinning `npx @dk/tinman@x.x.x`, or take the default `npx @dk/tinman` and let the migration prove itself. Note it would arrive as a subcommand, per the rule that Tinman gains commands and the assistant only proposes them, which takes the named command set past seven and needs that scenario updated in the same pass.
- **The test for whether anything belongs in the model. Operator, 2026-07-27: simpler is always better, and the model needs enough resolution to drive and capture the TUI or CLI and to make assertions against it. Nothing beyond that.** So a role, an attribute or a keyword earns its place by carrying something one of those three genuinely needs, and is deleted otherwise. This replaces taste, fashion and a reviewer's frown as the criterion, and it is applicable without re-running the conversation that produced it.
- **Applied, and both candidates survive.** `menu` is not a spacing convention: the model carries `selected`, "whether the region is the currently selected or highlighted one among its siblings", and `activate` works by moving that selection to a named item and confirming it, which `features/driver-session.feature:activation reaches an item the selection is not already on` depends on. A `list` has no selection to move, so `menu` is the resolution driving requires. **This also reclassifies the `top` fault: reading a status header as a menu is a discriminator bug, not a reason to delete the role**, since nothing in that header is selectable or activatable. `expect` in plans survives on the assert limb of the same test, now that `await` carries readiness and each keyword does one job.
- **Two qualifiers on that test, operator 2026-07-27, and they are not loopholes. Accessibility and standards conformance are always worth carrying.** **Accessibility is a consumer**, so the capture limb covers what a listener needs and not only what a current scenario asserts: `dim` is how a terminal says disabled, `hidden` how it says password field, `strikeout` how it says done or deleted, and a model dropping them returns to flat text for precisely the distinctions that matter most to the second consumer these notes already rank near first. **Standards supply vocabulary and semantics, never a completeness mandate**, which is the line Captain crossed: borrowing ARIA's role names rather than coining our own is always right, and implementing every SGR attribute because ECMA-48 enumerates it is a different act wearing the same clothes. Use a standard; do not set out to complete one.
- **Third qualifier, operator 2026-07-27: interfaces people already know from Playwright, webrat, Capybara and Selenium are always worth it, and familiarity beats minimality where they conflict.** `features/driver-session.feature` already declares this in its own opening, wanting the same semantic verbs a browser driver offers so a terminal test reads like the interaction a person would perform, and `activate`, `fill`, `press` and `close` are that borrowing working. The limit is the one the same feature states: borrow where the concept transfers, and do not force one that does not, a terminal having no pointer to click. **Caught by this immediately: Captain named the new readiness verb `await`, which is a JavaScript keyword, so a JS consumer would write `await driver.await(...)`.** Playwright says `waitFor` and Selenium says `until`; renamed to wait-for. Minimality picked the short word and familiarity picked the right one.
- **`doubleUnderline` remains the one that fails the test**, refined rather than reversed by the qualifiers above: it is underline with a second line and names no state a listener needs announced. Captain added it because ECMA-48 names it. Delete it unless a scenario or a listener needs it.
- **First deletion candidates, to judge once tables land.** `menu` and `menuitem`: the outside review found menu-against-list is decided convention rather than derivation, and the operator saw it produce `menu` holding `menuitem "days,"` from `top`'s header, which is a status line. If tables, lists and status regions carry everything `menu` was recognising, it has no honest case left. `doubleUnderline`: Captain added it because ECMA-48 names it, not because a scenario needed it, which breaks the rule above on the same day it was cited.
- **Tinman attempts no backward TOM compatibility. Operator ruling, 2026-07-27, and it is a licence rather than a restriction.** The model iterates freely and accumulates no legacy cruft: an operator wanting an old TOM runs an old Tinman, and everyone else updates their scantlings to the current one. No deprecation path, no compatibility shim, no version field negotiating behaviour at runtime. **The versioned `$id` is the whole mechanism**, which is why `every published schema URI names the packaged version` matters more than it reads: each release's schemas stay addressable at `@vX.Y.Z` forever, so pinning a version pins a model. A schema change therefore owes a version bump, and the bump is the entire compatibility story rather than a chore beside it. This licenses the per-property style fix, the role additions, and anything else the model needs, with no migration owed to anybody.
- **The opposite of NIH is the standing rule.** Operator, 2026-07-27. Where a modern, well-maintained crate, library, standard, convention or best practice exists, use it. Invent only what has not been invented, and only where adopting the existing thing would conflict with a need, a goal or a design, the bias toward modern ways and means included. A decline carries a fact, never a taste, and the corpus shows what that looks like: `vt100` was unmaintained and failed cell addressing and reverse video; `wezterm-term` is better and is not on crates.io while we publish; `mdbook test` compiles Rust doctests and cannot run a Tinman plan; `ratatui-testlib` owns spawning and ships no sandbox; Tinman is not a tldr client because the client specification carries flags, platform resolution, language handling and cache maintenance that are all off-mission. Where no such fact exists, adopt. This generalizes the two narrower rules below, and unlike them it reaches production dependencies, protocols, file formats and interface conventions as well as scantlings and checkers. **Before writing anything that sounds like infrastructure, search for the crate first.**
- **Prefer a concise attestation on a scantling over verbose behavioural scenarios**, and audit freshly authored specs against that, not only inherited ones.
- **A scantling based on a well-known standard beats a bespoke one**, and **prefer specifications over implementations**. Applied: JSON-RPC 2.0, WAI-ARIA, JSON Schema 2020-12, CommonMark info strings.
- **Prefer an independent tool over a bespoke checker**, and never let two checkers hold one rule.
- **`Rule:` prose carries durable context only.** Requirements belong in scenarios.
- **Tinman is Playwright, webrat, Capybara and Selenium for CLIs and TUIs, with a modern AI-era spin.** Operator framing, 2026-07-27, and it is the reference class to draw on. When a problem here looks new, check first whether browser automation solved it already: network interception, HAR record and replay, projects as a matrix, trace viewing, role-based locators. Borrow the proven design and spend the invention on the part that is genuinely new, which is a nondeterministic subject measured as a rate.
- **Tinman must stay useful for plain command-line testing**, not only full-screen work.
- **An idle deck is worse than a wrong call. Operator, 2026-07-27, and it outranks Captain's caution.** Their words: the consequences are not as bad as the latency of coming back and finding it sitting there. So dispatch first and finish the edits while a role is working, rather than sequencing every fix before anything moves. A watchbill edited under a running QM has already caused the older-copy recovery twice, and that recovery is cheap: the watchbill is not spent and the next QM takes the remainder. **Where Captain is weighing whether to dispatch, the answer is dispatch.** Reserve stopping for a real blocker.
- **Never split a voyage. Operator ruling, 2026-07-27, stated twice.** Voyage 10 opened at 13 watches and 59 targets and passed 25 and 106 in a single session, and the answer both times was to sail it whole. So a subsystem that does not exist yet gets built inside the voyage that specified it, and QM raising "these need their own voyages" is answered by keeping them. The cost is accepted knowingly: custody waits for the whole thing to go green, so proven work sits uncommitted longer.
- **No backlog debt.** Do it, decline it, or name its pivot. **User sharpening 2026-07-27: ask what deferring teaches us.** A pivot is a dependency, so waiting for it changes what gets built: tier-2 markdown testing genuinely cannot be written before `tinman fixture new` exists. A deferral with no dependency under it teaches nothing between now and later, and the work arrives at the later date in the same state, minus the context the person holding it had. Where Captain wants to defer, the test is whether any fact will differ when the item comes back. If none will, that is not caution, it is latency, and Captain is managing its own discomfort.
- Breaking changes are acceptable at 0.1.x, so a correct reshape does not wait for a major version.
- **Specify the floor as well as the ceiling.** A scenario asserting a measurement against a threshold also carries a null control. `all nine` and `all thirteen` are floors, so a glob reading nothing fails rather than passes.
- **Captain runs in the main loop, and the internal roles are dispatched away from it.** Operator preference, 2026-07-27. QM, Crew and Boatswain go out as context-isolated subagents; Captain keeps the human-facing seat and narrates while they are away. Captain never clears the main session in order to become QM.
- **Anything Tinman submits to somebody else's project gets a human review first.** User-stated 2026-07-27, prompted by the tldr-pages contribution and binding on any like it. Evidence from a sandbox strengthens the submission; it never substitutes for the review. Captain drafts and stops.
- **Auto-memory is off**, `autoMemoryEnabled: false` in both settings files. Anything worth keeping goes to a `.feature`, a scantling, `AGENTS.md`, `RIGGING.md`, or here.

## The matrix idea. Discovery only, 2026-07-27. Nothing authored, nothing binding.

**Tinman gets no matrix noun at all. Superseded 2026-07-27 by the operator's observation that promptfoo or similar will likely be the thing running Tinman.** Captain coined "campaign", then argued its way to "matrix" on legibility, and both were answers to a question that belongs to somebody else's tool. Read from the promptfoo docs rather than recalled: `providers` is the matrix, `evaluateOptions.repeat` is the repetition, `assert` is the expectation, and results are reported per cell of test times prompt times provider. All of it exists. Building `matrix:` into a Tinman plan would reinvent an eval harness, which is exactly what the anti-NIH rule forbids, and it would also break the operator's likely real workflow by assuming Tinman is the outer loop.

**The division of labour that replaces it.** The harness owns the matrix, the repetition, the assertions, the aggregation and the report. Tinman owns what no harness can do: drive a real coding agent in a PTY inside a sandbox, proxy and record its network, read the screen as a semantic model, and hand back observations. **The framing worth keeping: Tinman makes a coding agent addressable as a provider.** promptfoo evaluates prompts against model APIs; nobody drives a real agent program and measures what it did. That is the whole contribution and it is upstream of any matrix.

**The interface is already specified for us, which is the anti-NIH rule paying out.** A promptfoo custom provider implements `id()` and `callApi()` and returns `{ output, error, tokenUsage: { total, prompt, completion }, cost, cached, metadata }`, with token usage and latency recognised and surfaced as metrics. Non-JS subjects are supported through script, HTTP and Python or Go provider forms, so `tinman driver` and its JSON-RPC protocol are already the right shape and need no new surface. Note what the proxy buys here: a normal provider reports the token counts its SDK hands it, while Tinman reports what actually crossed the wire, which is a stronger number than the harness gets from anything else plugged into it.

**The eval and observability landscape is inspiration, not a roadmap. Operator, 2026-07-27.** Take ideas, conventions and inventions from it, and make Tinman usable by those tools' users, without bloating Tinman. Concretely this forbids: a per-vendor adapter or plugin for any of them, a vendor SDK as a dependency, a feature Tinman grows only because a platform expects it, and any integration that assumes Tinman is the outer loop. Nineteen tools were studied down to **two surfaces**, and two is the whole permitted footprint: Tinman is **driven** as a subject, and Tinman **emits** observations in a standard shape. Anything a specific vendor needs beyond those two belongs in that vendor's ecosystem, written by whoever wants it, not in this crate.

**What the study found, read from docs rather than recalled.** The observability platforms converged on OpenTelemetry: Phoenix is OTel-native and both emits and accepts OTLP with `gen_ai.*`, Langfuse ingests OTel spans, MLflow's tracing is OTel-native with GenAI semantic conventions, and Laminar, Langfuse, LangSmith and Phoenix are interchangeable from an ingestion standpoint under standard instrumentation. That collapses roughly eight of the nineteen into one emission. The eval harnesses take Tinman as a subject, and promptfoo showed the shape: an identifier, a call, and a response carrying `output`, `tokenUsage` and metadata, with script and HTTP forms for non-JS subjects, which `tinman driver` already satisfies.

**Two gaps, both unresolved and neither guessed at.** The OTel GenAI semantic-convention page has moved to its own repository and the old URL only redirects, so the stability levels are unread. And there are two competing attribute vocabularies, OpenTelemetry's `gen_ai.*` and Arize's OpenInference, with Laminar and Langfuse accepting the latter over the same OTLP exporter. The wire protocol is settled; the attribute vocabulary is not.

**Both questions are now measured rather than open. 2026-07-27.**

**Weight, counted in a throwaway crate so the tree was untouched.** Tinman today locks 438 packages. `opentelemetry-semantic-conventions` **alone is 1 package with zero dependencies**. The API plus SDK plus semconv is 44. Adding `opentelemetry-otlp` with `--no-default-features --features http-json` reaches 72 and pulls `prost` only. Adding it with default features reaches 114 and drags in `reqwest`, `hyper` and `tower`, which means a second HTTP client beside the `ureq` already shipped. **The default exporter is the bloat and the semconv crate is not.**

**OTel is declined, not scheduled. Operator, 2026-07-27: a wrapper would be a different project, so this is deferred for ever.** Under "do it, decline it, or name its pivot", this is the decline. It carries no pivot and belongs on no roadmap. A future Captain finding this section should read it as closed history explaining why Tinman ships no telemetry exporter, never as work waiting to be picked up. **What survives the decline is the observation stream below**, and it survives on its own merits rather than as preparation for anything: promptfoo needs it, any harness needs it, and it would be owed if OTel had never existed.

**The earlier reasoning, kept only as the record of how it was decided.** Not the exporter, not the SDK, and not even the zero-dependency semconv crate. It ships as a separate `tinman-otel` wrapper consuming the driver protocol. The weight numbers above are what made the wrapper look optional; the deciding argument is different and stronger. The GenAI conventions are still experimental and the space is fragmenting, so a separately versioned wrapper absorbs that churn without touching Tinman's cadence, and nobody has to choose `gen_ai.*` over OpenInference on every user's behalf, because a wrapper per vocabulary is a small independent piece of work anyone can do. Keep the vocabulary reasoning below as guidance for whoever writes the first wrapper; it is no longer a decision Tinman makes.

**What base Tinman owes instead, and it is the whole of the work.** Not an exporter: a complete, structured observation stream. Checked against `scantlings/driver-protocol.schema.json` on the day this was decided, the protocol is strictly request and response, with `$defs` holding `request` and `response` only and no notification form, and `result` carrying `ok`, `session`, `failure`, `screen`, `tom`, `backend` and `items` with no telemetry of any kind. **A wrapper can only export what the stream carries**, so tokens, turns, per-call latency and the skill-in-context facts have to be first-class protocol data or every wrapper is blocked regardless of vocabulary. The mechanism needs no invention: JSON-RPC 2.0 already defines a notification as a request carrying no `id`, which is the standard way to stream per-call telemetry as it happens rather than aggregating it into a final result. That is an additive change to the protocol scantling and acceptable at 0.1.x.

**This collapses the two surfaces into one, which is the real prize.** promptfoo drives the driver as a subject and reads its telemetry; a wrapper reads the same notifications and emits spans. One protocol, two consumers, no vendor code in the crate. Anything proposing a second surface should be read as bloat until it argues otherwise.

**Vocabulary: emit `gen_ai.*`, not OpenInference.** OpenTelemetry's GenAI conventions carry a large well-governed community and a clear trajectory toward becoming the ecosystem default; convergence is expected to run toward OTel, and Arize AX normalizes `gen_ai.*` spans to OpenInference at ingestion, so emitting the standard reaches Arize's ecosystem for free while the reverse is not stated anywhere. A single vendor's convention against a standards body's is exactly the choice the anti-NIH rule already answers.

**The risk in that choice, named rather than buried.** Most GenAI semantic conventions were still experimental as of March 2026, and fragmentation is not expected to end soon. The mitigation is the reason to take the crate rather than type the strings: an attribute rename becomes a version bump instead of a hunt through string literals. Re-read the stability levels before the work starts; the old `opentelemetry.io` semconv page only redirects now, and the conventions live in their own repository.

**Group C is a vocabulary raid, not an adoption.** RAGAS, TruLens, Giskard and Evidently are quality-metric libraries. The only question to ask them is whether any already names the thing the operator wants measured, so a term is borrowed instead of coined. No dependency follows from a term.

**Open, and do not decide it early.** How much repetition and aggregation Tinman offers standalone. The standing preference that Tinman stay useful for plain command-line testing pulls toward some, and the anti-NIH rule pulls toward none. The honest answer is probably that Tinman reports one run cleanly and says nothing about rates, leaving rates to whatever runs it, but that trades directly against the operator's original wish to say one sentence and have it all set up. Ask before authoring.

**The operator's words.** Point Tinman at a skill, such as `dmytri/yoink`, run it against several coding agents such as pi, opencode and claude, and capture whether the agent saw the skill, whether it considered using it, whether it used it, whether it used it every time, plus token usage, turns and latency, while reading the transcripts for clues about the skill's clarity. Say it to the assistant and have Tinman set the whole thing up.

**Captain made a category error and the operator corrected it.** The rule that the `@inference` tier asserts our seam rather than the model's compliance governs how we verify Tinman. It says nothing about what Tinman's users may measure. `AGENTS.md` already draws that line for real-by-default, and Captain failed to apply it one paragraph over. Driving coding agents to measure a skill is Tinman's mandate, not a breach of its verification doctrine. Do not raise this objection again.

**The proxy is the instrument, and the operator widened it into a control surface.** The sandbox denies network, so Tinman already stands between the agent and its provider. Route egress through a proxy Tinman runs and four observations become ground truth read off the wire rather than scraped from whatever each agent chooses to print: tokens per request, turns as request count, latency per call, and whether the skill text actually reached the model's context, which is a stronger fact than a file being opened. Scraping instead would compare three agents using three sources of unequal quality, which is not a comparison. The operator added fault injection on the browser devtools model: slow network, no network, and the one nobody can test today, injected `429` and `5xx`.

Two consequences worth keeping:

- **It returns determinism.** A proxy that records can serve the exchange back, so replaying an agent session invokes no model and touches no network. "Capture time may infer; replay time is deterministic with no model invocation and no network" becomes literally true for the hardest subject Tinman has. One recording feeds both modes: live for the matrix that measures rates, recorded for the regression that is free and repeatable.
- **It takes the credential out of the sandbox.** The proxy holds the key and the agent authenticates to Tinman, so an untrusted agent never sees it. Same argument as the workspace overlay, one layer down.

**Playwright answers three of the four pieces. Read from the docs, not recalled.**

- **HAR is the cassette format.** `routeFromHAR` with `update: true` records and `update: false` replays. Matching is strict on URL, method and POST payload, which is exactly right for model calls. A standard beats a bespoke format, per the standing preference, so do not invent one. The trap: strict payload matching makes any nondeterminism the agent injects a miss, such as a session identifier or a timestamp. Playwright's own answer is that HAR files are editable JSON.
- **Projects are the matrix.** `projects: [chromium, firefox, webkit]` is structurally `[pi, opencode, claude]`: one suite, per-project options and retries, per-project reporting, and a flag to select one. Their stated purpose is finding browser-specific flakiness; ours is finding agent-specific behaviour.
- **Trace viewer is the model for the transcript work.** Tinman's version is richer, because a TOM snapshot is semantic where a DOM screenshot is not.
- **The fourth piece is the spin, and Playwright has no answer for it.** It retries a flaky test until green and labels it flaky, which is a binary verdict with an asterisk. A rate is not a retry. "Used it always" is a proportion over N runs, and that vocabulary is ours to design.

**The open question, unanswered.** Does a matrix hand back a report, or a plan carrying rate expectations such as "the yoink skill is used in at least 8 of 10 runs against opencode", which reddens the day a model update moves the answer? The `inspect` ruling is the precedent: an explainer tells the operator what happened and is believed or not, while a probe writes a plan they commit and watch go red. A regression test on agent behaviour appears to be a thing nobody ships.

**Pivot.** Nothing is authored and nothing should be until voyage 10 is spent. The first buildable slice is the proxy, because every other piece depends on it and it stands alone: egress through a Tinman-run proxy, the exchange recorded as HAR, the credential held outside the sandbox. The matrix and the rate vocabulary follow it. Ask the open question above before authoring any of it.

**Open that slice with a crate survey, per the anti-NIH rule.** A recording HTTP proxy with fault injection is infrastructure, and infrastructure is exactly what the rule says to search for before writing. Three things to price against the registry rather than build: the proxy itself, HAR serialization, and latency and error injection. Read the registry with `cargo search`, never recall, and judge maintenance and recency as the rule's "modern and well-maintained" test requires. Invent only the part the survey shows is genuinely missing, which is most likely the rate vocabulary and nothing below it.

## Scheduled work

Every item names its pivot and its role. No "someday".

**Settled 2026-07-27: stream roles are decided by shape.** A program that exits on its own is read as a stream, its complete output unbounded by terminal height; a program still running is read as a screen. For the stream's roles the user chose shape over a blanket rule: any blank-line separated **multi-line** block present makes it a `log` of `article`s, and otherwise it is a `list` of `listitem`s. The discriminator is deliberately multi-line, so a stray blank line between filenames leaves a listing a listing. The deciding argument was the accessibility one: the right role is the one a human would need spoken aloud, and that genuinely differs between `ls` output and a compiler's diagnostics, so a single rule is wrong for one of them in the way a listener notices immediately. Carrying both roles nested was considered and declined against "a role is added when a scenario needs it". Authored as watch11.

**Next harbour, Shipwright.**

- **Audit the dependency set against the anti-NIH rule, in both directions.** The rule is new as a stated rule and the tree predates it, so harbour is where it reaches the existing choices. One direction asks whether anything hand-rolled here now has a maintained crate behind it. The other asks whether any current dependency has gone stale, which is the same test `vt100` failed. Read the registry with `cargo search` rather than recall, and report findings; adoption stays Captain's call with the operator.
- **Constrain locator roles to the TOM role set** in `harness-plan.schema.json` and `driver-protocol.schema.json`. Today `role` is a free string in both, so a plan writing `role: statusbar` validates and then matches nothing, failing at replay time with the operator gone. User decision 2026-07-27: both schemas, at harbour, rather than riding voyage 10. It is a breaking change to two published schemas and needs its own scenario; the existing scantling-enumeration scenarios join the new enums to production with no new machinery.
- **Possible behaviour-stale plank.** `src/driver.rs` `launch` carries `@planks("the failure reports the selection did not reach the {string} named {string}")`. It joins a current pattern so the string join cannot see it, but `launch` is the session-start seam and that step asserts an activation failure. Harbour's coverage triage is the net.
- **`src/driver.rs` reads 0.00% in every tier** while its 24 binding scenarios pass. `tests/cucumber/support.rs:952` SIGKILLs the child, and a killed instrumented process never flushes counters. No coverage claim about the driver means anything until this is closed.
- **`forbidden-doubles` keys on the type names `Mock|Fake|Stub|Dummy`.** `LocalProvider` is a real double, correctly marked, that the rule would not have caught unmarked. Any double named otherwise is invisible.
- **Derive a schema-property-against-production-field check.** A scantling only validates the shape production emits, so a declared property production never emits, or emits and never reads, is untested in both directions.
- **Decide `ratatui-testlib`'s harness layer.** Leaning no: launch must happen inside bwrap and the PTY runner accepts only a prepared process, while `TuiTestHarness` owns spawning and ships no sandbox.

**After the fixture subcommand exists.** Markdown testing tier 2, executing fenced plan blocks, tagged by CommonMark info string. Tier 1, the parse check, landed on voyage 10 and subsumed the bespoke skill-to-parser check. Doc examples elide and need fixtures to be honest, so tier 2 waits on `tinman fixture new`. **mdbook was declined**: `mdbook test` compiles Rust doctests and cannot run a Tinman plan, so the extractor is ours either way; revisit as a marketing call, never a testing one.

**The adversarial-inspection thesis, user-stated 2026-07-27, and it is the sharpest framing the project has.** A `--help` text, a man page, a tldr page and an inference engine all produce *claims*. Tinman does not accept claims; it runs them in the sandbox and reports what happened. That is the categorical difference between Tinman and the projects tldr-pages names on its wall of shame: they republish model output about commands nobody executed, Tinman executes them. It also collapses two requirements into one, since trying arbitrary command lines written by strangers is only sane inside isolation. Sources rank by proximity to the binary: `--help` is ground truth from the binary in hand, `man` opportunistically where the sandbox has one, tldr as a hint. Nothing outranks what the binary says about itself. Authored as watch11.

**Tinman is a testing tool, not a command explainer.** User framing 2026-07-27, and it decided the shape of watch12. An explainer tells the operator what a command does and is believed or not; a probe that writes a plan hands them evidence they commit, re-run, and watch go red the day the program changes under its own documentation. So the product of running documented examples is a plan, not a verdict. Captain's first pass had inspection reporting claims as "refused" or "hint", which is explainer-shaped output, and the user's framing is what caught it. The two phases are unchanged and reached from a new starting point: documentation supplies the hypotheses where a human supplied them under `record`, and replay stays deterministic either way. **Only an honoured example earns an expectation**, since writing a refused one as passing manufactures this project's signature fault and writing it as a failing expectation hands over a permanently red plan describing someone else's stale docs.

**The wall of shame is not what Captain assumed.** Checked rather than recalled: `https://github.com/tldr-pages/tldr/wiki/Clients` lists sites "using LLMs on TLDR pages without crediting the project", with output "often inaccurate and riddled with LLM hallucinations". It is not about hammering servers or client-spec breaches. Tinman's exposure is therefore real and specific, and two things answer it, both authored on voyage 10: the pages are CC-BY-4.0 so credit rides in the written plan the operator commits, and no page text reaches an artifact on the page's authority, because the deterministic pass refuses any name the screen does not independently carry. **Tinman is also not a tldr client** and will not become one: the client specification carries required flags, platform resolution, language handling and cache maintenance, all off-mission, so Tinman asks the operator's installed client instead and keeps no cache of its own.

**Platform policy, operator 2026-07-27: Linux only now, macOS in the near future, Windows never, WSL is the Windows answer.** Windows-never is a permanent simplification worth banking: no ConPTY as a second terminal layer, no `cmd.exe` quoting, no path divergence, and WSL is Linux so it costs nothing to support. macOS needs a sandbox backend of its own before it can ship, which is product work rather than packaging, and `AGENTS.md` already phrases the constraint to expect it, "the only Linux backend is Bubblewrap".

**The concrete gap the policy exposes: `resolve()` has no platform gate.** `src/backend.rs` returns Bubblewrap for `Backend::Auto` on any platform, and no `cfg(target_os)` appears anywhere in `src/`. On macOS that means a clean compile, a successful resolve, and a failure later when the binary is missing, where the honest behaviour is the unsupported-backend error the enum already carries. It is the same fault as a green npm install on an unsupported platform: a path that looks available and cannot honour itself. Small to specify and worth doing before either release target exists.

**Packaging follows the policy rather than the convention.** npm's `os` field declares the supported platforms so the registry refuses the install itself, which is honest, where a clean install followed by a runtime failure is the thing this project keeps deleting. Ship Linux-only and declare it; add darwin when the backend exists; never publish a Windows artifact.

**npm binaries beside the crate, operator 2026-07-27, at the first GitHub release.** Packaged so `npx @dk/tinman` runs the current version with no install step. **The unversioned form is the invocation**; `npx @dk/tinman@x.x.x` is the deliberate opt-out for somebody holding onto an old TOM, and the default form should say so, because a project that presents pinning as normal looks like one that expects you to hold back. **This is the no-back-compat ruling made executable**: where somebody does pin, the same version string pins the binary and its schemas at `@vX.Y.Z/scantlings/`, so an operator wanting last year's TOM runs one command and fetches contracts from a matching URI. Checked 2026-07-27: `@dk/tinman` is unpublished, a clean 404, and `tinman` on crates.io is ours at 0.1.2. **Confirm the `@dk` scope is claimable before committing to the name**; an npm scope needs the user or org to exist, and the unscoped package `dk@1.0.5` does not block it but was not the thing checked.

**A second outbound target owes its own ship and verify lines in `RIGGING.md`, per the Outbound policy, and targets deploy independently.** The npm one is strong by construction: `npx @dk/tinman@x.x.x --version` fetches, unpacks and executes the published artifact in a single command, which is end-to-end verification rather than a lookup.

**Noticed while checking, and it is the same fault as everything else today: the crates.io verify does not verify.** `cargo search tinman` confirms the name resolves in the index. It does not confirm the crate installs, runs, or is what we shipped, and the Outbound policy asks for the artifact a user consumes. `cargo install tinman && tinman --version` is the honest form. Fix it when the npm target is added, since both lines get written in the same pass.

**Outbound, blocked on project age rather than on us.** CONTRIBUTING sets the bar: "The project has been maintained for at least a year before submitting a page for it," waived for notable projects at maintainer discretion. Tinman is 0.1.x, so **the next release is too early** and the item re-files against a later one. Two things to have ready by then, both cheap: the page itself in their house style, and the honest framing for the PR body. Their AI policy closes pull requests "suspected of being made in whole or in part through generative AI... without human-review," because such output is "often inaccurate" - and a page whose every example was executed in a sandbox before being written is the strongest available answer to that objection. It is an argument to make to humans in a pull request, never a licence to automate the submission. **User ruling 2026-07-27: a human reviews the page and the pull request body before submission, in every case and with no exception.** Sandboxed evidence raises the floor on accuracy; it does not stand in for the review their policy asks for, and a project whose whole thesis is that a claim gets checked before it is repeated does not submit unreviewed text to somebody else's repository. Draft it, hand it over, wait. Contribute a `tinman` page to tldr-pages, `https://tldr.sh`. User direction 2026-07-27: use and incorporate tldr in both directions, so that Tinman is visible there and so that Tinman leverages what is there. The consuming direction is authored on voyage 10 as watch10; the contributing direction is a pull request to the tldr-pages repository and belongs with a release rather than with a voyage, since a page describing an unreleased command surface is the drift problem pointed the other way. Write it after `man` and `completions` land, so the page describes the seven-command surface.

**Undecided, no pivot yet.** A clap-derived machine-readable command manifest, which is what an external coding agent should read instead of parsing prose help. Waiting to see whether the subcommand set grows enough to describe.

**Next release, whoever ships.** `sandbox-spec.schema.json` lost the `fixture` values from `home` and `env.*.from` at the 2026-07-25 harbour, a breaking change to a published schema, so the release carrying it owes a version bump. The bump still has no owner in doctrine; see the gaps below.

**Held, not scheduled.** `jsonschema` 0.48.5 and `generic-array` 0.14.7 sit behind latest with 0 semver updates available. The `locked` policy holds them until a spec or a Captain decision moves them.

## The false-green pattern. The most expensive thing this project has learned.

Every instance was green while asserting nothing. **The test: when a scenario goes green early and cheaply, ask what it would take to make it red. If nothing would, the scenario is the defect.**

The older ones, in brief: a perturbation discharged by deleting its line; a `menu` role the deterministic pass already produced; a launch answering `ok:true` before the program started; a recorded plan carrying no expectation; two activation scenarios asserting text the fixture drew anyway; a scantling that is valid JSON and an invalid schema; a `Given` that seeded nothing, leaving its `Then` asserting against ambient state.

The ones still worth reading:

- **The gplint glob.** `features/**/*.feature` matches zero files, because `**` must consume at least one directory segment and `features/` is flat. It exits 0 having linted nothing. Working values are `features/**` and `features/*.feature`.
- **A plank string shared across six seams.** `@planks("a process is prepared and launched")` is carried by `process.rs`, `sandbox.rs` three times, `bwrap.rs` and `pty.rs`. The string join is green no matter which of the six is dead.
- **An optional schema property is an attestation that cannot fail.** Read the `required` list, not the property list, when asking whether an attestation binds. This is why `cursor` is required in `assistant-ui.schema.json` while it is optional in `tom.schema.json`.
- **A schema can promise a capability production silently discards.** `harness-plan.schema.json` declared `cwd` on a `run:` step while `RunForm::Full` had no such field, and serde dropped the key without a word.
- **A `required` property proves a field exists. Nothing proves it is read.** Crew added the field, left it `None` everywhere, read it nowhere, and threaded a sidecar anyway. Closed by `requiredReferences` in `pty-sandbox-boundary.json`.
- **`tinman replay` was declared and did nothing**, swallowed by a wildcard match arm. **A wildcard match arm is the production form of a glob that reads nothing.** Closed by `command-dispatch-completeness.json`.
- **An absence assertion disarmed by a change to the asset it names.** Captain grew an asset from one line to two and a contiguous-string absence assertion could never fail again. **Absence assertions are the ones that go quiet, because nothing about their output changes when they stop testing.**
- **A focused run against a scenario name that no longer exists selects zero scenarios and exits green.** **A focused run reporting `0 scenarios` is a failed selection, never a pass.** Voyage 10's watchbill was generated by extracting scenario names from the feature files rather than typed, for this reason.
- **A refusal scenario green because the command does not exist.** `completions refuses a shell it cannot emit for` passes today: `tinman completions klingon` exits non-zero because `completions` is not a command at all, so the scenario reports complete while asserting nothing about a refused shell. QM caught it and named it incidental. **Re-run it once the command exists**, or it is green through the whole voyage that builds the thing it claims to test.
- **`mandoc -T lint -W error` exits 0 on an empty file.** Captain hit this live: `tinman man` did not exist, the redirect wrote zero bytes, and the lint reported clean. A validity check with no non-empty floor passes hardest on nothing at all. Closed by `And the emitted page is not empty`.
- **An isolation assertion whose target never reached for the secret.** `record launches its target inside the sandbox` asserted a recorded snapshot did not carry `TINMAN_SECRET` while the fixture never tried to print it. Green for the whole time record ran unsandboxed. Rewritten on voyage 10 with the target printing a marker beside its attempt.

**Corollaries.**

- **Zero coverage on a negative-control branch is the control working, never evidence of dead code.** Captain read `pty::launch` at count 0 and ruled it dead; Shipwright refused and was right. Before removing any zero-count code, find the assertion that would go quiet with it. **`pty::launch` and `pty::capture` are both kept**; a future harbour re-deriving "dead code in `src/pty.rs`" is repeating a settled decision.
- **A scantling only ever tests the shape production emits.** A property production never emits is untested; a property it emits and never reads is also untested, and that one survives a `required` list.
- **Where durable context changes and no scenario reddens, write the scenario that reddens.** Reach for a perturbation only where behaviour genuinely cannot be pinned.
- **A ruling that widens what one layer owes narrows what every other layer may claim.** Re-read every scenario counting on the definition in the same pass. Voyage 10 moved three counts: proof contracts four to five, versioned URIs fifteen to sixteen, dialect-declaring scantlings unchanged at ten. Verified by command before writing them.
- **A green from a subagent is a claim until a command answers it.**

## Rules of the language and the tooling

- **A `Background:` belongs to the `Rule:` it sits under.** Adding a `Rule:` above a `Background:` orphans it for every scenario after. This reddened seven scenarios once. No feature file does it now.
- **gplint's `indentation` rule is off deliberately.** It expects scenarios to nest one level under a `Rule:`. The house style is flat 2/4 throughout.
- **gplint's dupe-name option is `in-feature`, not `in-file`.** One Feature per file makes them the same thing here.
- **Read the registry before naming a version.** `cargo search <crate>` answers it in one command. A `curl` to crates.io from this VM returned nothing for three crates that all exist; `cargo search` found every one. Trust the CLI, not the socket.
- cucumber-rs makes `--tags` and `--name` mutually exclusive. Tag exclusion rides `CUCUMBER_FILTER_TAGS`; `--name` selects the scenario. Encoded in `RIGGING.md` `focused`, so the anchored name does the tag-exclusion job.
- Runner is `tests/cucumber.rs`, `harness = false`, `fail_on_skipped()` so undefined steps redden.
- No clean cucumber-rs dry-run, so `discover: none`. To prove specs parse, run the default tier and read the feature count.
- Read the `result` field before trusting a weather line: `101` is a cargo test failure, so a fast line with `result: 101` is an early abort wearing a duration.
- **Count the directory rather than trusting a report's prose.** Two roles independently called `scantlings/verification-conformance` a three-rule set when `ls` reports four.
- Env confirmed: rustc 1.97 (edition 2024), bwrap 0.11.2, user namespaces enabled, node 22.23.1. `.env` holds a live `TINMAN_API_KEY`, git-ignored, so `@inference` is runnable and costs money per run.

## Design decisions that bind (user-confirmed)

- **The assistant never gains capabilities. Tinman gains subcommands, and the assistant proposes them.** "Help me build a fixture" is not the assistant writing files; it is Tinman growing `tinman fixture new`, proposed and confirmed through `parse_command_line` like every other action. The blast radius of a confused model stays the enumerable set of subcommands, and every capability remains usable by a human with no model in the loop.
- **One surface, three consumers: a human at a terminal, Tinman's own assistant, and someone else's coding agent.** Nothing is assistant-only, so discoverability is the whole game. `SKILL.md` is the one prose source, read directly by agents and rendered for humans. `mandown` was declined: rendering the skill as roff puts it on the drift-prone side.
- **The skill and the man page are not one document.** A man page is exhaustive reference, a skill is teaching material. What unifies them is their sources: the CLI surface from clap, the concepts from prose.
- **`--help` is ground truth** for a program under inspection, from the binary being tested, at the installed version, inside the sandbox. **`man` opportunistically**, absent from a minimal bwrap sandbox. **tldr, including `tlrc`, as hints only**, never as the basis for an assertion, on the drift argument alone; the network objection was wrong and the user caught it, since network is denied to the target and not to Tinman. Voyage 10 authored the consuming half against inference naming, where a stale page costs a worse suggestion the deterministic pass then refuses. The `--help` ground-truth half is still unauthored and rides with the `inspect` stream-versus-screen answer Captain owes.
- **Every driver call except `launch` names its session, and an omitted session is an error rather than a default.** A driver holds several sessions at once, so defaulting is a guess.
- **Tinman is a driver, not only a CLI.** `tinman driver` speaks newline-delimited JSON-RPC 2.0. The YAML plan stays canonical for recorded flows. The protocol is RPC, not a second test format.
- **TOM is the DOM equivalent and inference is codegen.** The deterministic builder is the spine and produces every addressable role. The LLM engine is a second producer of the same shape, capture time only, proposing **names, never roles**.
- **Resolution and confirmation are two operations.** Resolution answers what a locator matches and reports ambiguity as ambiguity. Confirmation runs at capture time only.
- **Terminal size is a property of the run, never of the plan.**
- **Plan YAML grows with the test.** Shorthand removes typing, never adds capability, never weakens a default. An omitted `sandbox:` block means secure defaults, not no sandbox.
- **`home: fixture` and `env.from: fixture` are struck.** `mounts` is the live provision for reaching a tree outside the bound set.
- **The `@inference` tier asserts our seam, never the model's compliance.** Shaping the request is not validating the response.
- **Help text is an asset, not a scenario.** Copy lives in `assets/help/`.
- **Inference: any OpenAI-compatible provider, OpenRouter the default.** `TINMAN_API_KEY`, `TINMAN_BASE_URL`, `TINMAN_MODEL`. Environment wins over `.env`. The credential is vendor-neutral by name so the default is never lock-in.
- **Tier placement.** `@sandbox` marks scenarios that need a real sandbox to run at all, which after voyage 10 includes every `record` and `inspect` scenario that launches. `@inference` is real paid calls, never on the inner loop. Retag a scenario rather than weaken a tier policy.
- **Emulator is `alacritty_terminal` 0.26.0.** `vt100` was unmaintained and failed cell addressing and reverse video. `wezterm-term` is better and disqualified: not on crates.io, and we publish. `alacritty_terminal` is 0.x and breaks across releases; budget for that.
- **Scantlings go into the assistant context, features do not.** Measured: `features/` 94794 chars, all scantlings 37960, the six user-facing ones 22087, `SKILL.md` 6025. The argument that decides it: a scantling cannot drift, because a `@contract` scenario pins it, while the skill can and did.

## Two doctrine gaps worth raising upstream

1. **A release version bump has no owner.** The write-scope list gives Shipwright the manifest for dependency work only, and Shipwright is a harbour role while a release is mid-outbound. Captain bumped `Cargo.toml` to 0.1.2 under Captain's authority at sea, recorded as a departure. Boatswain named the same gap independently.
2. **An adoption proof leaves no durable trace.** A green looks identical proven or not. Every proof so far was made in a session since cleared, so the tree carries no record that any check can fail. The run record has a slot-shaped hole here: it records that a target passed, never that it was ever observed to fail.

## Prior art and the accessibility angle

`ratatui-testlib`, crates.io, MIT, v0.1.0, single maintainer, framework-neutral despite the name. It occupies Tinman's transport layer via the same `portable-pty` and carries no semantic model, no sandboxing, no replay, no inference. Snapshot testing was considered and rejected: `insta` asserts "same as last time", which is drift detection rather than specification.

**Accessibility is the TOM's second consumer**, and after voyage 10 it is closer to being the first. Terminal accessibility is an empty category: VTE exposes a flat `AtkText`, Windows Terminal a UIA `TextPattern`, so an ncurses menu reaches a screen reader indistinguishable from a paragraph. A black-box model deriving ARIA roles, accessible names and now computed presentation from an attributed cell grid is the missing piece. Two free consequences: a wrong role passes a test but a listener notices at once, so holding the model to what a human would need spoken aloud improves it for testing; and never route the TOM through a lossy intermediate, because attribute-derived structure needs a genuine attributed grid.

Keep the framework-neutral naming: `assets/skill/SKILL.md` says "CLIs and full-screen TUIs" and names no framework.

## Reference docs, not sanctioned artifacts

`intent/idea.md`, `intent/isolation.md` and `intent/help.md` are historical intent sources, each carrying a banner saying so, and the whole `intent` directory is in the crate's `exclude` list. Do not treat them as requirements and do not report their staleness. Two scantling descriptions cite them as the source of an invariant, `assistant-command-boundary.json` and `pty-sandbox-boundary.json`, which is provenance rather than a live reference.
