{
  "watch1": {
    "scenarios": [
      "features/semantic-capture.feature:capturing every item collects past the visible window",
      "features/semantic-capture.feature:captured items keep their screen order",
      "features/semantic-capture.feature:items repeated across scroll positions are collected once",
      "features/semantic-capture.feature:capturing the visible scope reads only the current window",
      "features/semantic-capture.feature:capturing a pane that never stops scrolling fails within its budget"
    ]
  },
  "watch2": {
    "scenarios": [
      "@logic"
    ]
  },
  "watch3": {
    "scenarios": [
      "@sandbox"
    ]
  },
  "watch4": {
    "scenarios": [
      "@inference"
    ]
  }
}
